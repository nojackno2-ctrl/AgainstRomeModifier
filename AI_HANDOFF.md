# AI Handoff - Live Project Memory

## Endless spectator mode feasibility (2026-07-18, static design evidence only)

- User asks for an endless-mode spectator option where the normal player slot is controlled by computer AI. No feature code has been implemented and no installed-game directory was accessed.
- Static EXE evidence shows the single-player branch of `FUN_00502e00` initializes team 0 as human and teams 1..7 as CPU through `FUN_0054ccd0`; the backing CPU-control byte is `DAT_00628b20[team * 10]`, exposed to scripts as `s_lgcGetTeamCpuCtrl` / `s_lgcSetTeamCpuCtrl`. This gives a concrete EXE control point for making team 0 CPU-controlled.
- Endless startup currently calls `SetPlayerTeam(0)` (`FUN_0044ceb0`), while the same setter explicitly accepts values above 7 as sentinel teams and the game startup uses team 8 before selecting a real team. A true spectator prototype should therefore attach the local observer to sentinel team 8, not leave the user on CPU team 0 where normal ownership checks could still permit commands.
- `ak_level.bci` independently reserves team 0 by using occupied-mask base `1` in single player; `pickTeam` otherwise has CPU mask `255`. A complete prototype must change that base to `0` so the normal party lifecycle can allocate and respawn team 0. Changing `team.dat` alone is insufficient; it only supplies faction/population/banner data and has no human/CPU field.
- Required runtime gates before shipping: confirm no player trek is created for sentinel team 8, prevent immediate loss/end-state, define spectator fog-of-war/camera behavior, prove team 0 receives a normal AI village/party and participates in cleanup/respawn, and prove Apply/Restore exact-signature safety on all five ENDL scripts plus the EXE. Keep any first implementation Experimental and new-game-only; do not claim feasibility as runtime-verified yet.

## Village-AI self-production total quota diagnosis (2026-07-18, static evidence only)

- Implementation complete: user authorized a direct 3x version. Added standalone Experimental `VillageGarrisonQuota3x` backed by P20 on `Dorfverteidigung.bci`. P20 redirects the four exact `s_searchImportantPos` calls to one appended same-script helper without shifting existing control flow; the helper calls the original symbol and multiplies the returned count by 3 with VM opcode 34, whose native dispatcher case is confirmed as signed integer multiplication. Detection requires all four exact call sites plus one exact helper/call-target state; Unknown states are refused. Disable restores all four external calls, removes the 88-byte helper, adjusts BCI `codeSize`, and returns both synthetic and workspace-original decompressed input byte-identically. The toggle is shown in the AI card, marked Experimental, and deliberately excluded from Enable All pending runtime verification. Canonical README/TechDoc/reverse-engineering/opcode/schema documentation is synchronized. Verification: focused feature tests passed 14/14 before adding the local-original round-trip; the workspace original is detected as Original and passes in-memory Original -> Ultimate -> Original; full Release solution build passed with 0 warnings/errors; full xUnit passed 262/262; schema JSON, Python AST, `git diff --check`, and Chinese-doc BOM checks passed. No installed game directory was accessed or modified. Fresh endless-game behavior and long-run performance remain pending and must not be claimed runtime-verified.
- User reports that village-style AI appears to stop self-producing troops after reaching a total and asks whether the cap can be removed. No code change was requested or made in this diagnostic pass.
- Fresh read-only disassembly of the workspace original `SYSTEM/CLAK/SCRIPT/Dorfverteidigung.bci` confirms that AI Ultimate M1's existing four-site `6,6 -> 20,20` patch changes members per newly created squad only; it does not raise the number of squads a village maintains.
- The actual per-type squad targets are dynamic: the main handler calls `s_searchImportantPos` for types 1, 2, 3, and 4 at code offsets `0xD350/0xD37C/0xD3A8/0xD3D4`, then copies the resulting matched-position counts into quota variables 60/62/64/66 at `0xD7DC..0xD814`. Before each of the four `s_addNPCJob_createUnit` calls, it counts the team's existing squads of the corresponding figure type and starts a replacement job only while the current count is below that type's ImportantPos-derived target (`0xF114..0xF390`). The village's total self-produced garrison cap is therefore the sum of its four defensive ImportantPos counts, not one global literal.
- Follow-up quota inventory: all 42 workspace-original `Endlos_*_Siedlung*.sdl` files across `ENDL_000..004` were decompressed read-only. None serializes `OD_IPOS01..04` as SDL object text; they list the settlement buildings, while `s_searchImportantPos` observes important positions after those objects are loaded into the engine. Therefore the exact squad total is runtime/configuration-dependent and cannot be reported as one static template literal. The confirmed current fixed value is 20 members per newly produced squad; total squads remain `count(IPOS01)+count(IPOS02)+count(IPOS03)+count(IPOS04)`, with each type maintained separately.
- Technically the modifier can raise this boundary by replacing the four copied quota values with bounded configured targets. A truly unbounded loop should not be shipped: the job path is sequential, but unlimited squads can still consume civilians, population, simulation time, and save size, and excess squads have no verified defensive-position assignment. Prefer an Experimental bounded multiplier or explicit per-type cap, preserve exact Original/Ultimate/Unknown signatures, and runtime-test fresh endless games before promotion.

## Default unlocked fire/poison arrows (2026-07-18, removed after two runtime rejections)

- Second runtime rejection and current objective: after fully closing the game, reapplying the new BCI build, relaunching, and testing again, the user confirmed basic attacks still use ordinary arrows. This disproves the `s_setObjActWeapon` index-1 replacement as sufficient. The user explicitly requested removal of the feature. Remove its `FeatureKey`, profile property, UI, preset, normal apply/detect behavior, advertised docs, and enable tests. Retain only narrowly scoped restore-only migrations for the already-shipped BCI literal 1 and the earlier EXE getter patch, so the next Apply/Restore returns both to Original. Do not attempt a third implementation.
- Removal milestone: the FeatureKey, registry/profile property, Combat-card control, localization label/tooltip, preset wiring, feature detection, enable path, README/TechDoc/schema advertisement, feature-matrix row, and RE helper script are removed. `RetiredDefaultSpecialArrowsMigration` restores only the exact BCI weapon-literal-1 output; the EXE model likewise exposes only a restore planner. Both run from normal Apply and Stats Restore, while the BCI cleanup also runs in startup-safe migrations. Original/unknown/mixed files remain untouched. Focused removal, registry/UI, EXE legacy, BCI byte-exact cleanup, and Apply/Detect/Restore integration tests passed 93/93; an incremental build showed the existing unrelated ArgmTrace nullable warning. Full solution validation remains next.
- Final verification for removal: full Release solution build passed with 0 warnings/errors and full xUnit passed 257/257. The retired BCI migration test proves exact literal-1 cleanup returns the compressed script byte-identically to Original; the EXE restore-only model remains covered. The generated modifier must be used once with normal Apply (or Stats Restore) to clean a game directory that received either rejected experiment. No installed game directory was accessed or modified by this work.
- Root cause and replacement milestone: `SYSTEM/CLAK/SCRIPT/ak_krieger.bci` initializes every warrior through `s_setObjActWeapon(currentObj, weaponIndex=0, update=1)`, downstream of the EXE command-mode global. Native `FUN_005424f0` validates the requested weapon via `FUN_00542630`/`FUN_00542690` and `FUN_004c0de0(unit, weapon)` before writing the active-weapon byte. The replacement patch changes only this unique weapon literal from 0 to 1, requires exactly one recognized Original/Patched signature, and uses the shared BCI cache. The rejected getter-only EXE state is now restore-only but remains an enabled migration signal so the next Apply installs BCI and returns all three EXE sites to Original. Synthetic BCI round-trip, refusal, detector, legacy migration model, legacy-selection preservation, registry, and full Apply/Detect/Restore focused tests passed 94/94. One unrelated existing `ArgmTraceFeature.cs` nullable warning appeared during the test build. New BCI runtime behavior is not yet claimed.
- Final automated verification after the BCI replacement: full Release solution build passed with 0 warnings/errors; full xUnit passed 258/258. The earlier incremental nullable warning disappeared in the clean solution build. Schema JSON parses and focused tests remain 94/94. Runtime proof is still pending and must use the newly built modifier, fully close the game before Apply, then relaunch so `ak_krieger.bci` is reloaded.
- Runtime rejection: after applying the feature, the user confirmed Roman ranged units still use ordinary arrows for their basic attack even though Roman fire arrows are available without research. The initial BSS/getter default-mode transform is therefore insufficient and must not be claimed working. Trace the attack-command construction and special-arrow button side effects for a later reset or additional per-unit state before revising the patch; do not repeat the getter-only approach without new evidence.
- User objective: ranged units that have already unlocked fire arrows or poison arrows should use that special projectile mode by default when attacking, while retaining the ability to switch back to normal arrows.
- Fresh static EXE analysis established the UI/runtime chain: `igm_but_brandgeschoss` and `igm_but_giftgeschoss` both call `FUN_0044e960(1)`, normal ranged mode calls `FUN_0044e960(0)`, and raid mode calls `FUN_0044e960(2)`. `FUN_0044e960` stores the global mode at `0x00736A58`; `FUN_0044e980` reads it. Attack dispatch maps returned modes 0/1/2 to command subtypes 1/2/3. Fire arrows and Hun poison arrows therefore share mode 1; the unit ability state determines the actual effect. User-supplied gameplay evidence corrects the initial faction assumption: Roman ranged units also have fire arrows, available by default without research, so Roman fire-arrow behavior belongs in the runtime test matrix.
- The original mode global is in the zero-filled PE BSS, so it cannot be changed by a data-byte edit. Safe reversible design: redirect the six-byte getter at file offset `0x4E980` into its adjacent original 22-byte alignment cave at `0x4E96A`; map never-set raw 0 to special mode 1, raw sentinel 3 to normal mode 0, and preserve raw 1/2. Change only the normal-arrow button's pushed raw value at `0x413FC` from 0 to sentinel 3. This preserves explicit normal, special, and raid selections while making the untouched startup state prefer special mode.
- Ghidra one-shot output is local ignored evidence at `re_workspace/ghidra_special_arrow_probe.log`. The repo-local Ghidra completed despite its known Unicode-path Log4j warning; the separate supposedly clean Temp install is currently missing `Ghidra/Framework/Utility/Module.manifest` and must not be trusted without repair.
- Implementation must use exact Original/Patched/Unknown detection across the normal-button byte, getter bytes, and cave bytes; unknown or mixed states must be refused. Keep the feature Experimental until in-game verification confirms unlocked units fire the expected projectile and locked/unsupported units retain safe ordinary behavior. No installed game directory was read or modified during this analysis.
- Implementation complete: added `DefaultSpecialArrows` as a registered Stats toggle and experimental Combat-card control; wired PatchProfile, EXE composition/detection/restore, bilingual localization, Disable All, and exact three-site state handling. The getter trampoline uses raw sentinel 3 only for explicit normal mode, maps untouched raw 0 to special mode 1, and preserves raw special 1 / raid 2. Canonical README/TechDoc/reverse-engineering/schema documentation records the patch and its runtime gate. Focused model, detector, registry, UI-preset, and full modifier Apply/Detect/Restore integration tests passed 91/91. Full Release solution build passed with 0 warnings/errors. Full xUnit passed 252/255; the same three pre-existing unrelated `ArgmTraceFeatureTests` still expect the old `version.dll` payload while current code installs `winmm.dll`. Schema JSON parsed, `git diff --check` passed, and Chinese canonical docs retained UTF-8 BOM. Do not claim runtime behavior yet.

## Roman reinforcement complete village handoff (2026-07-18, runtime-verified complete)

- New runtime evidence: the user started a fresh endless game and Roman reinforcements still retreated. This disproves the opcode-120 helper output as sufficient for the requested gameplay result.
- User explicitly changed the target to complete handoff: soldiers, pack horses, and civilians must all transfer to the village; preserving the vanilla type-1 delivery/dissolve path is no longer required.
- P9 Ultimate now emits `jz(117)+0` at the state-49 type filter so every reinforcement object enters the zero-quota donation path. The existing corrected opcode-120 helper remains mandatory and runs `s_setScriptMode(0)` before `s_setObjMark(recipientMark)` for each donated object.
- Original remains `jz+92` plus direct mark. The former `jnz(118)+92` split, recognized direct-mark outputs, and exact broken opcode-160 helper state are Legacy and converge to `jz+0` plus the opcode-120 helper. Unknown filter/helper/call-target states remain refused.
- P8 remains 70. This team-unit threshold can suppress a later reinforcement spawn after retained units accumulate, but it cannot order an already spawned reinforcement party to retreat. Complete handoff deliberately makes pack horses/civilians count toward that long-term gate.
- Runtime verification: after applying the complete-handoff output, the user confirmed in a fresh endless game that the modification succeeds: Roman reinforcement soldiers, pack horses, and civilians are handed to the village and no longer retreat. This closes the visible handoff runtime gate. Long-duration behavior after retained units approach P8=70 was not separately claimed.
- Automated verification: focused Release tests for P9 five-map round-trip, M6 migration, save repair, and Apply/Restore passed 16/16. Full Release solution build passed with 0 warnings/errors. All tests outside the concurrently changing `ArgmTraceFeatureTests` passed 243/243. The unfiltered suite passed 248/251; its three failures are the known unrelated `version.dll` expectations while the current argm-trace work has switched to `winmm.dll`.

## Fix Roman reinforcement soldiers disappearing at the village (2026-07-18, superseded by complete-handoff policy)

- Root cause of the failure: Although the helper call was successfully changed to `callint` (opcode 120) previously, the original trailing external call setup `argc -3` (opcode `73, -3`, 2 words) and `call` (opcode `86`, 1 word) were left behind in the BCI bytecode. When the helper returned, the VM continued execution on these leftover bytes without the necessary symbol on the stack, causing VM execution failure/halt.
- Solution: Patched the trailing `73, -3` to `112, 4` (`jmp 4`) to bypass the leftover `86` (`call`) instruction, letting it reach `71` (`pop`) safely. Restored it back to `73, -3` when disabling the patch.
- Wildcards in `DonationActionSignature` within `CustomPatches.cs` and signature variables in unit tests were updated to support both the Original and patched (`112, 4`) patterns.
- Verification: Updated all affected tests in `verify_split_patches.cs` and `EndlessSchedulerDeadlinePatchTests.cs` to assert the correct bypass logic. Run `dotnet test` and confirmed all 249 tests passed successfully.
- Reverification on 2026-07-18: the focused P9/M6/save-repair Release tests passed 15/15 and the full Release solution build passed with 0 warnings/errors. The then-current full xUnit run passed 248/251; all three failures were confined to concurrent `ArgmTraceFeatureTests` still expecting the old `version.dll` payload while the working tree had switched the feature to `winmm.dll`. Do not attribute those unrelated failures to P9, and do not claim the current whole-tree suite is green until that concurrent work is reconciled.
- Original notes on the P9 fourth control point:
  - New runtime rejection: the user showed the modifier UI with all normal features enabled, including `RomanReinforcementGarrison`, and reports that both reinforcement soldiers and pack horses disappear. Treat the current helper-backed implementation as failed in game; re-decompile the actual patched output and do not attribute this to unchecked settings. Two initial read-only reconstruction attempts produced no valid patched sample (PowerShell could not load the P9 type; system `python` was a broken uv trampoline); both were rejected by a `changed/Ultimate/codeSize` gate. A minimal `re_workspace/p9_reanalysis` C# inspector now references `src.Core` directly for repeatable output reconstruction; use the bundled Codex Python for `bcitool.py`.
  - Root cause found by re-decompiling modifier-generated output and the EXE VM dispatcher: the appended helper call site used opcode `160`, but dispatcher case `0xA0` pops a stack value and calls `FUN_005BC830 -> FUN_005BC740`, the typed array allocator (`arrCreate`); it is not a function reference. Native internal calls use opcode `120`, with `target = callSite + 8 + operand` (for example state-49 `0x17FAC` resolves exactly to `0xABF8`). The broken opcode meant the helper was never called with valid VM semantics, explaining why green byte-self-consistency tests did not predict runtime behavior. P9 now emits `120`, recognizes the exact old `160` + helper/target state as Legacy, and migrates it on Apply; disable accepts either recognized state and restores the original direct `s_setObjMark` bytes. Independent reconstruction of all five ENDL samples passed a raw-byte gate (`callOpcode=120`, target exactly `0x1C22C`) and re-decompiled to the correct helper. Focused P9/save/apply tests passed 11/11; full Release solution build passed with 0 warnings/errors; full xUnit passed 249/249; verifier and P9 inspector builds passed; schema, `bcitool` syntax, and `git diff --check` passed. No installed game/save was read or modified. Corrected runtime behavior still requires the user to re-Apply with the new executable and start a new endless game, or repair the embedded script in an existing save.
  - Historical acceptance criterion, superseded by the complete-handoff request above: pack horses were not to accumulate and `jnz+92` preserved their vanilla state-32/34 delivery. Keep this only as prior-attempt evidence; it is no longer the current target.
  - New in-game report: Roman reinforcement units appear to disappear immediately rather than visibly retreat. Do not treat the earlier visual description "soldiers garrison / pack horses retreat" as sufficient proof that the same visible unit objects persist.
  - Follow-up observation resolves the visual ambiguity: every supply pack horse is converted into an ordinary villager, while every soldier disappears immediately at the village without approaching the retreat point. This rules out ordinary state-50 exit cleanup as the explanation for the soldiers.
  - The pack-horse conversion matches the decoded state-32/state-34 delivery lifecycle: objects delivered within 400 of the village use `s_sendMsg(6, ...)` plus `s_addNPCJob_dissolveUnit`, followed by civilian recreation. The soldier failure is instead on the P9 donation path.
  - The most likely current defect is the previously documented but prematurely dismissed script-mode caveat: P9's state-49 branch only re-marks donated squads through `s_setObjMark`; it does not call the known release helper `0xBBC0`, which performs `s_setScriptMode(0)` plus the handoff message. The new runtime observation is consistent with squads remaining in party mode and then being removed/hidden during party cleanup, but static evidence alone still cannot distinguish exact `DELETE_PARTY` destruction from an engine-side invisible conversion.
  - Current static control flow has two materially different disappearance paths: state 49 "donation" only re-marks an object through `s_setObjMark`, while state 50 orders remaining party objects toward the exit and calls `s_destroyObj` once they are within 100 units of it, before `DELETE_PARTY` removes objects still carrying the party mark.
  - The existing `argm-trace` records spawn/village/unit creation but not `s_setObjMark`, `s_destroyObj`, party state, object identity, position, or mark changes. The current report therefore cannot yet distinguish (a) normal exit cleanup near an off-map/nearby exit, (b) a re-marked soldier converting into a non-visible garrison representation, or (c) a wrong branch leaving soldiers marked for state-50/`DELETE_PARTY` destruction.
  - Implemented P9's fourth control point: the donation branch keeps its existing size but changes the direct external `s_setObjMark` call into an internal call to a helper appended at the BCI code-section end. The helper performs `s_setScriptMode(0, x, y)` and then `s_setObjMark(recipientMark, x, y)`, releasing the soldier squad before assigning it to the settled type-4 party.
  - Detection now requires the exact helper bytes and call target for Ultimate. The previous `jnz+92` direct-mark output is Legacy and auto-migrates; malformed helper bytes/target are Unknown and refused. Disable removes the helper, restores the direct call, adjusts `codeSize`, and returns all five ENDL samples byte-identically to Original.
  - Save repair now reconstructs an expanded decompressed `scr.dat` payload instead of assuming the embedded BCI length is unchanged. Focused P9 migration/tamper tests passed 2/2; P9 + Save Repair + Apply/Detect/Restore integration tests passed 16/16. Full Release solution build passed with 0 warnings/errors, full xUnit passed 249/249, the independent verifier project compiled, schema JSON parsed, and `git diff --check` was clean. No installed game or save was touched. Fresh-game runtime verification is still required before claiming visible soldier garrison behavior is fixed in game.

## GitHub branch consolidation into `主要開發` (2026-07-18, complete)

- User authorized merging every GitHub branch into `主要開發`, then deleting each successfully integrated source branch.
- After `git fetch origin --prune`, `claude/github-error-resolution-9ifcdy` was already fully contained in `主要開發`, and `claude/village-ai-rome-reinforcement-pbdktk` pointed at the same commit as `主要開發`.
- `claude/game-operation-logging-4hs36b` contained five commits not on `主要開發`; its code and documentation merged automatically. The only conflict was this append-only handoff, resolved by preserving both the village-AI diagnosis and the branch's 2026-07-18 runtime-promotion, Roman-reinforcement, and argm-trace records.
- Merge commit `1a189b8` passed the documented Release solution build with 0 warnings/errors and the full xUnit suite with 247/247 passing; `git diff --check` was clean and this file retained its UTF-8 BOM.
- Pushed `主要開發`, verified every source tip was an ancestor and the GitHub main tip matched locally, then deleted all three integrated remote branches. A final `git ls-remote --heads origin` showed only `主要開發`; the merged local tracking branch was also removed with safe `git branch -d`. The user's `build/` directory was preserved and is ignored by the merged `.gitignore`.

## Village-AI Rome reinforcement report: diagnosis session, no code change (2026-07-17)

- User report: village-type AI Rome reinforcements still wrong — soldiers retreat even with the M6 threshold at 70, or pack horses pile up in the village. Full audit of the current chain (P9 site-9 `[66,0]` + filter `jnz(118)+92`, P8 threshold 70, `BciFeatureDetector` keeping Core/M6 Legacy selected, `EndlessSaveAiRepairService` migrating embedded P6/P8/P9) found it complete and internally consistent on `主要開發` tip `8446ef0`; no code defect was identified and nothing was changed.
- Key diagnosis: the briefly-shipped same-day "restore-vanilla-filter" combination (site 9 `[66,0]` + vanilla `jz+92`) produces BOTH reported symptoms in one game — squads (type != 1) always stay in the retreat array (soldiers retreat) while type-1 pack horses fall into the zeroed quota and are all donated (horses pile up every wave). Current `P9.Detect` already classifies that combination as Legacy and migrates it on Apply. The user's symptoms therefore match stale runtime states (old modifier build, vanilla-restored installed maps, or old save-embedded scripts), not the current implementation.
- Recovery sequence given to the user: take the current build (8446ef0), re-Apply with M6 enabled (auto-migrates all shipped legacy states), then start a NEW endless game, or run Save Manager "Repair Endless AI" on an existing save; already-donated pack horses are live save objects and cannot be removed by migration — if they already exceed the threshold, restore a pre-pileup backup.
- Boundary confirmed from existing RE records: the only donation walk (v56 read `0x18168`) and the file's only `s_getUnitType` call (`0x1825C`) are inside the type-5 handler, and the type-5 spawner serves teams with a settled type-4 party. The handler that visits pure type-1 villages (possibly type-3 parties) was NEVER decoded, has no donation logic to retarget, and cannot be patched from existing records without violating the "never rewrite undecoded BCI control flow" rule.
- Unblocking requirement if pure villages still misbehave after a clean new game: the repo contains no BCI sample, but `tools/bcitool.py` can decompress/disassemble in-session. Needed inputs: one installed `MAPS/ENDL_000/SCRIPT/ak_level.bci`, plus ideally a symptom save's `CLAK/scr.dat` to identify the visiting party's `v47` type before decoding that handler.
- Environment note: this Linux session has no dotnet SDK, so no build/test was run; the audit is static. The remote feature branch `claude/village-ai-rome-reinforcement-pbdktk` was already merged into `主要開發` and deleted on origin.
## Runtime promotion: centered high resolution + entire-map unit vision (2026-07-18, complete)

- User confirmed in-game that `高解析度置中（4:3）` and `所有單位全地圖視野` both work as intended.
- Removed the stale Experimental labels from both localized toggle names. `AllUnitsEntireMapVision` documentation now records its primary gameplay effect as runtime-verified; long-running AI targeting behavior and performance remain explicitly unverified.
- Follow-up: both toggles are now explicitly enabled by `BtnEnableAll_Click`; Disable All already cleared them. Added a UI regression that invokes the Enable All handler and asserts both toggles are checked.
- No patch, detection, restore, profile, or installed-game behavior changed. This milestone promotes the already-wired Stats/Compat toggles, updates the one-click preset, and synchronizes README zh/en, TechDoc zh/en, known-patches, the feature verification matrix, localization, and this handoff.
- Validation complete for the Enable All follow-up: focused UI regression passed 1/1, full Release solution build succeeded with 0 warnings/errors, and full xUnit passed 247/247. Preserve the concurrent `argm-trace` and `RomanReinforcementGarrison` work already present in the shared working tree.

## Standalone Roman reinforcement garrison feature (2026-07-18, complete)

- Historical promotion objective: move atomic P8 + P9 out of the user-facing `EndlessAi.M6` identity into standalone `RomanReinforcementGarrison`. The original soldier-only scope is superseded; the current runtime-verified label and behavior are 「羅馬增援完全移交」 as recorded above.
- Safety boundary: do not split P8 from P9. P8 keeps the reinforcement unit threshold high enough for retained squads, while P9 donates soldier squads and sends type-1 pack horses/civilians away. The old `EndlessAi.M6` ID must remain a migration alias so existing detected/profile state is not silently disabled.
- Preserve the unrelated uncommitted `argm-trace` feature work in shared registry/UI files. Runtime verification still applies only to the already-confirmed P8/P9 byte behavior; this UI/identity promotion needs build/tests before completion.
- First focused test attempt stopped at compile with `CS0136` because the new M6 migration branch reused the later `out bool enabled` local name in `GetEndlessAiModule`; renamed the first variable to `legacyModuleEnabled`. No test body ran in that attempt.
- Implementation milestone: `RomanReinforcementGarrison` is now a registered Compat toggle wired through PatchProfile, BCI detection, apply/restore, localization, UI layout, and Enable/Disable All. `EndlessAi.M6` is specialized (no UI toggle) and normalizes into the new key; the orchestrator still applies P8 + P9 together. Focused registry/core/integration tests passed 14/14 after the compile-name fix.
- Canonical docs updated so README zh/en, TechDoc zh/en, feature verification matrix, known-patches, and `data/game_schema.json` describe the standalone feature and migration alias.
- Verification complete: focused registry/core/integration tests 14/14; full Release solution build 0 warnings/errors; full xUnit 246/246; schema JSON parses; `git diff --check` clean. No installed game directory or save was accessed. The identity/UI migration is statically verified; the underlying P8/P9 runtime behavior retains the 2026-07-17 in-game confirmation.

## argm-trace MSVC build + host tests green (2026-07-18)

- User installed the VS 18 Community "Desktop development with C++" workload. Built `version.dll` with the VS-bundled CMake: `cmake -S native/argm-trace -B build/argm-trace -A Win32` then `--build --config Release`. Output: 20,992-byte Win32 DLL, machine 14C/x86, export table undecorated and matching the genuine version.dll (verified with dumpbin /exports).
- Two compile fixes were needed: (1) the naked forwarding thunks in `proxy_version.cpp` collided with winver.h's own `extern "C"` prototypes (C2733) — renamed the thunks to `Thunk_<name>` and mapped them back to the real export names via `EXPORTS name = Thunk_name` in `argm_trace.def`. (2) Nothing else; detour/targets/tracelog compiled clean under /W4 /permissive-.
- Host tests are now wired into CMake behind `-DARGM_BUILD_TESTS=ON` and run via CTest on the SAME MSVC x86 toolchain. Added `tests/detour_mechanism_test_win.cpp` (native Win32 variant of the mmap-shim Linux test; exercises real `src/detour.cpp` against genuine VirtualAlloc/VirtualProtect). Both `lde_test` and `detour_mechanism_test` pass (2/2, 100%). lde_test now covers all seven real hook-target prologues (all decode to >=5 relocatable bytes, no relative branches). detour test confirms stub arg capture `[5 1 4 0 0 20 20 8 3]` / `[3 3 9 1 2 15 40 0 0]`, trampoline behavior preservation, and clean RemoveAllDetours.
- ONLY remaining open item: live-game smoke run. This writes `version.dll` into the game install dir, so per project policy it is the USER's step: drop the DLL beside Against_Rome.exe, run once, copy the `[build]` TimeDateStamp (expect 404D1710) into `argm_trace.ini`, then reproduce an endless session and confirm ai.spawn/ai.village/ai.active events + stability. Do NOT deploy into the install dir automatically.

## argm-trace byte-verification pass (2026-07-18, conventions verified from installed EXE)

- Parsed the installed `Against_Rome.exe` PE (read-only) with a scratch Python script: `TimeDateStamp=0x404D1710`, ImageBase `0x00400000`, code section `AUTO` VA 0x1000 = raw 0x1000, so raw offset = VA − 0x400000. Dumped all six hook targets' real bytes.
- Resolved all three RE open items from `runtime-trace-hooks.md`:
  (1) `0x00547F50` / `0x00549500` argument order and stack convention confirmed from prologues (args are plain dwords at `[esp+4+4(n−1)]`; village arg1=team 0..7, arg2=non-null template ptr; early-out `C3` ret = caller-cleaned) — existing log column labels were correct.
  (2) **`bci.op` hook was wrong**: true dispatcher entry is `0x005B1C60` (`53 56 57 55 89 E5 ...`), and the VM context is stack argument 1 — `EBX` is only loaded at `0x005B1C6F`, so the old `FmtOpcode` reading `rb.ebx` at `0x005B1C62` would have logged the caller's stale EBX. Fixed: hook moved to `0x005B1C60`, context read from `cs[1]`. `exe-functions.md` and `bci0-opcodes.md` corrected too.
  (3) This machine's install already carries the force-Roman patch (`0x45BD60` starts `53 6A 03 5B 90`), so the stock faction signature can never match there. Added a second signature/formatter (`factionF`) for the patched prologue; both variants are attempted, at most one matches.
- Also fixed a real bug: `kFactionMask` was 22 chars vs a 21-byte signature (out-of-bounds compare in `VerifySignature`); mask now keys on the instruction skeleton with rel32/abs32 wildcarded.
- Hardened: every build-locked address hook now ALSO carries a byte signature dumped from the analyzed EXE (npcjob/npcactive/village/createunit/bciop), so a wrong address inside a matching build gets refused. `FmtVillage` now logs the template name string (SEH-guarded `SafeReadStr`). `argm_trace.ini.sample` documents `expectedTimeDateStamp=404D1710` for the analyzed build. `lde_test.cpp` extended with all seven real prologues + 3 opcode cases.
- Toolchain blocker: this Windows machine has NO C++ compiler (VS 18 Community without the C++ workload; no gcc/clang). The DLL and the host tests could not be compiled here; the 2026-07-17 host-test pass ran on a Linux host with `g++ -m32`. Next actions: install the VS C++ workload (or run tests on a host with g++), build Win32 `version.dll`, then live-capture.

## Runtime flight-recorder DLL `native/argm-trace` (2026-07-17, implementation complete; NOT built/run yet)

- New native subproject answering the user request "完全記錄遊戲中所有運作,包括增援電腦AI的動作,以便定位問題". It is a 32-bit `version.dll` proxy that loads into `Against_Rome.exe` and installs **log-only** inline hooks on the reverse-engineered AI callbacks, writing a time-stamped `argm_trace.log`. This is the runtime "flight recorder" complementing the offline save/BCI editing.
- Hooked (all documented in `docs/reverse-engineering/runtime-trace-hooks.md`): `s_addNPCJob_createUnit` impl `0x00547F50` (AI reinforcement spawn: team/mode/count range), `s_setNPCActive` `0x00548CE0` (defeat→respawn eligibility), `s_setVillageTemplate` `0x00549500`, `s_createUnitAndMems` `0x0052A020`, Roman faction selector `0x0045BD60` (verified 21-byte signature), and optional full BCI opcode stream at VM dispatcher `0x005B1C62` (off by default, very high volume). The `ai.spawn` fields map onto the `endless-mode-ai.md` party state machine, so reinforcement-stopped / slot-exhaustion problems can be localized from the log.
- Safety (matches repo "verify original bytes before writing" philosophy): (1) PE build-fingerprint gate — address-based hooks stay disabled until `[build] expectedTimeDateStamp` in `argm_trace.ini` matches the running build; only the signature-verified faction hook is exempt; (2) per-target byte-signature verification; (3) header-only length decoder `src/lde.h` sizes the stolen prologue and aborts the hook on any relative branch or unknown instruction; (4) register/flag-preserving naked stub that reads args off the caller stack without disturbing it (cdecl/stdcall safe), formatter faults swallowed by SEH. A refused hook is a no-op.
- Files: `native/argm-trace/{CMakeLists.txt, argm_trace.def, argm_trace.ini.sample, README.md}` and `src/{dllmain,proxy_version,config,detour,targets,tracelog}.{h,cpp}` + `src/lde.h`. Not part of `AgainstRomeModifier.slnx` (native, not .NET); `dotnet build/test` does not cover it.
- Verified on this Linux host (no Windows/MSVC/game available here — game files are gitignored and absent): (1) instruction-length decoder `tests/lde_test.cpp`, 46 cases incl. faction prologue, ALL PASS; (2) full hook mechanism `tests/detour_mechanism_test.cpp` compiles the REAL `src/detour.cpp` (Win32 calls shimmed to POSIX mmap via `tests/shim/windows.h`) with `g++ -m32`, installs a real detour over a 9-arg cdecl function mimicking `s_addNPCJob_createUnit`, and confirms the stub reads the exact args off the caller stack (`[5 1 4 0 0 20 20 8 3]`), the trampoline preserves behavior, per-call capture works, and RemoveAllDetours cleanly restores — ALL PASS. This validates the stub/trampoline/rel32/RegBank machine code. NOT yet verified: MSVC Win32 compile of the DLL, the version.dll proxy/PE-fingerprint/rebasing paths, and any behavior against the live game. Open items before claiming it works are listed at the bottom of `runtime-trace-hooks.md` (build with MSVC; confirm arg order of `0x00547F50`/`0x00549500`; confirm `EBX`=VM context at `0x005B1C62`). Do NOT claim runtime success until built and captured against the user's game.

## Extend endless-save repair for pack-horse pileup (2026-07-17, implementation complete; runtime pending)

- User screenshot `codex-clipboard-8623e832-b92c-4b8e-93de-76186c6e8a29.png` shows the Roman camp packed with donated supply horses and reinforcements stopped. This matches the embedded legacy P9 `jz+0` behavior: type-1 pack horses are donated every wave, remain team units, and eventually contribute to the P8 `s_searchTeamUnits(team)` reinforcement gate.
- The installed-map M6 detector fix below cannot update a running/old endless save because `CLAK/scr.dat` embeds its own `ak_level`. The existing opt-in Save Manager repair only migrated P6 scheduler timing, leaving embedded P8/P9 unchanged.
- Extended `EndlessSaveAiRepairService` to validate and atomically migrate embedded P6, P8, and P9 together. It still requires the caller to create a full save backup first, rejects any Unknown patch state before mutation, recompresses/verifies PFIL, and is idempotent.
- Updated the Save Manager action/confirmation/result text to "Repair Endless AI" and explicitly warns that already-donated pack horses are live save objects and are not removed by script migration. If their existing count still blocks reinforcement, the safe recovery is a pre-pileup backup or a new endless game.
- Regression fixture now models legacy P8 threshold 40 plus P9 quota 0/filter `jz+0`; repair reaches Ultimate for P6/P8/P9 and a second repair reports AlreadyRepaired. Verification: focused save/M6/P8/Save Manager tests 20/20; full Release solution build 0 warnings/errors; full xUnit 237/237. Actual repair of the user's save and post-repair runtime behavior remain pending.
- Forced cleanup feasibility review: current script evidence only exposes `s_getUnitType`, whose type 1 includes pack horses and civilians; no verified script API was found that identifies the exact `FigTiePac00_Packpferd` objdef. `TMEM`/`IARR` are task variables/object-handle arrays, not proven authoritative world-object records, so deleting handles would not safely delete entities. A cleanup feature requires a user-provided copy of the affected save for read-only format/reference analysis; do not implement a broad type-1 destroy because it can remove civilians or other legitimate units.

## Fix Roman reinforcements not leaving soldiers (2026-07-17, historical; superseded by runtime-verified complete handoff above)

- `RomanEndless` only forces the player faction to Roman; reinforcement handoff is the independent `RomanReinforcementGarrison` feature (legacy alias `EndlessAi.M6`, atomic P8 + P9). Current P9 emits site 9 quota `[66,0]`, type filter `jz(117)+0`, and the opcode-120 release helper so every reinforcement object is handed to the village.
- Fixed the concrete upgrade-state bug: `BciFeatureDetector` now keeps `Legacy` selected for both modules with an established migration contract, `Core` and `M6`. Prior shipped P8/P9 thresholds/filters therefore remain checked after loading current settings and the next Apply migrates them to current Ultimate instead of restoring vanilla (`jz+92`, quota 15) and making soldiers retreat.
- Existing endless saves are a separate persistence boundary: they embed `ak_level` in `CLAK/scr.dat`, so even a correctly patched installed map does not change an already-created save. No installed game/save path was accessed during this diagnosis; no runtime state has been inspected yet.
- Added an integration regression that builds a partial legacy M6 fixture, proves detection keeps `EndlessAi.M6` enabled, applies the detected profile, and verifies M6 reaches `Ultimate`. It failed before the fix at the enabled-profile assertion and passes afterward; focused M6/core/P8 verification is 11/11.
- Verification complete: focused M6/core/P8 tests 11/11; full Release solution build 0 warnings/errors; full xUnit 237/237. Runtime differentiation still requires a newly started endless game because existing saves keep their embedded script.

## Save Manager responsive UI rearrangement (2026-07-17, complete)

- Replaced the fixed 780/390 save-manager content coordinates with a responsive 68/32 `TableLayoutPanel`; the live-save and backup cards now split the left column evenly and retain a 10px gap from the preview/details card.
- Reduced the initial/minimum window sizes to 1180x780 and 960x640 so the form fits smaller desktop work areas. Both action rows now use proportional table layouts, the preview scales with its card, and save-grid columns use `FillWeight` rather than fixed widths that forced a horizontal scrollbar.
- The custom rounded window region is recalculated after resizing. Added `SaveManagerFormLayoutTests` to cover minimum-size visibility, action containment, responsive column ratios, and fill-mode grids.
- Verification: focused layout test 1/1; isolated TEMP-artifacts Release solution build 0 warnings/errors; full xUnit 236/236. Computer-use visual QA could not start because the app-launch approval timed out, so no runtime screenshot was captured; the minimum-size geometry is regression-tested but player-facing visual confirmation remains useful.
- Follow-up screenshot exposed a remaining 39px bottom overflow: `Size` had been assigned while the form still used the default sizable border, then `FormBorderStyle.None` changed the client area after anchored controls were created. The initialization now sets borderless mode first and assigns `ClientSize`; the layout regression also asserts that the root content bottom exactly matches `ClientSize.Height`. Follow-up focused test 1/1, isolated Release solution build 0 warnings/errors, and full xUnit 236/236 passed.

## Repair incomplete local Ghidra installation (2026-07-17, complete)

- Diagnosed the old machine-local `AgainstRome_RE\ghidra` tree as an incomplete/mixed Ghidra 12.1.2 installation: recognizer classes and multiple processor `.pspec` files are missing. This is not safe to repair by copying isolated classes.
- Downloaded the official NSA GitHub release `ghidra_12.1.2_PUBLIC_20260605.zip` and verified its SHA-256 exactly matches the release value `b62e81a0390618466c019c60d8c2f796ced2509c4c1aea4a37644a77272cf99d` before extraction.
- Extracted without overwriting the old tree to `C:\Users\nojac\AppData\Local\Temp\AgainstRome_RE\ghidra-12.1.2-clean\ghidra_12.1.2_PUBLIC`; the old incomplete tree remains present but is explicitly deprecated.
- Verification run 1: fresh one-shot PE import and 92-second auto-analysis completed; `GhidraUnitRangeSpeedAnalysis.java` resolved `moves`, `w1_rad1`, `w1_rad2`, their consumer functions/callers, and decompiled bodies; exit 0 with `Post-analysis succeeded` and `Import succeeded`.
- Verification run 2: fresh one-shot import completed; `GhidraScriptIniAnalysis.java` resolved `SYSTEM/cl_script.ini` and parser sections at `0x41BCE0`, while `GhidraRessAnalysis.java` resolved `SYSTEM/ress.ini`, `[objres]`, `[volkres]`, and cost storage/consumer functions; exit 0 with post-analysis/import success.
- Updated `decompilation-workflow.md` to use the clean Ghidra + `jdk21-full` and a disposable `%TEMP%` project, and updated the all-feature matrix to replace the obsolete toolchain-blocked caveat with fresh-run evidence.
- Distribution health audit: all 5,225 archive files exist after extraction with zero length mismatches; Temurin `java`/`javac` 21.0.11 both run; all failed/successful disposable project markers were removed by `-deleteProject`. Updated the remaining stale `exe-functions.md` failure note and historical handoff project guidance to point to the repaired one-shot workflow.
- Final repository checks after the repair documentation: FeatureRegistry focused tests 7/7, matrix still covers 44/44 IDs, `git diff --check` clean, and `AI_HANDOFF.md` UTF-8 BOM preserved.

## All-feature decompilation verification matrix (2026-07-17, complete)

- Audited all 44 `FeatureRegistry.All` entries and added `docs/reverse-engineering/feature-verification-matrix.md`, mapping every feature ID to its actual write target, decompilation/decoded-script evidence, static verdict, and separately recorded runtime evidence. `DgVoodoo` and `ToEnglish` are explicitly external deployment features; `EndlessAi.M2/M3/M4` are compatibility aliases normalized to `EndlessAi.Core`; the native widescreen EXE experiment remains runtime-rejected and the active path uses dgVoodoo presentation.
- Added a registry regression in `FeatureRegistryTests` that requires the matrix to cover every registered feature exactly once. Updated the reverse-engineering index and corrected stale `glory-upgrade-combat.md` wording now that `LeaderGlory` and `GeneralSkills` are managed bounded patches.
- Fresh Ghidra headless rerun was initially blocked by an incomplete local installation, then repaired on 2026-07-17 with the SHA-256-verified official 12.1.2 distribution. Fresh unit-range/speed, script-INI, and ress focused analyses now complete successfully; see the repair section above and `decompilation-workflow.md`.
- First focused test run: existing 6 registry tests passed, new matrix test failed because it tried to ascend from the TEMP artifacts output and could not find the repository. Fix: locate the repo from the compile-time `CallerFilePath` of `FeatureRegistryTests.cs`, which is independent of artifacts location. Focused rerun passed 7/7. Isolated TEMP-artifacts Release solution build passed with 0 warnings / 0 errors; full xUnit passed 235/235; `git diff --check` is clean; `AI_HANDOFF.md` UTF-8 BOM preserved. Preserve the unrelated in-progress P8/P9 endless-AI changes already present in the worktree.

## Fix reinforcement pack-horse pileup: P9 filter inversion CONFIRMED; P8 threshold back to 40 (2026-07-17, final)

- In-game report (ESAVE_001): Roman camp accumulated supply pack horses (`FigTiePac00_Packpferd`) every reinforcement wave. Root cause: P9's third control point shipped 2026-07-08 as jz operand `92 -> 0` (eat the type filter), donating EVERY unit of the retreating type-5 party — pack horses included — so they never retreated.
- Fix (FINAL, in-game confirmed on a new endless game): INVERT the filter — jump opcode `117 (jz) -> 118 (jnz)`, operand kept `92`. Soldier squads (type != 1) fall into the zeroed quota branch and are donated (garrison); type-1 units (pack horses / civilians) take the vanilla retreat path off-map. Consistent with the 2026-07-08 type-semantics record.
- Mid-day churn, kept for the record: the user first reported the mirror behavior (soldiers retreating, horses staying), which briefly produced a restore-vanilla-filter build; the user then corrected the observation (soldiers garrison, horses leave — inversion works) and the jnz build was reinstated. EXE decode of `s_getUnitType` captured along the way: callback `0x52a190`, returns 0 for flags bit `0x10`, 1 for bit `0x20`, else -1; flags byte at `0x2146086 + 312*idx`, classifier `~0x513d40`.
- Detection: Original is filter `jz 92` plus direct mark; Ultimate is `jz 0` plus the exact opcode-120 helper. Former `jnz 92`, recognized direct-mark states, and the exact opcode-160 helper output are Legacy auto-migrated. Signature wildcards jump opcode + operand words, but unknown combinations are refused.
- P8 reinforcement unit threshold: changed to `70` at the user's request (same-day history: 40 -> 30 -> 40 -> 70). `70` = Ultimate; `8`, `30`, `40` are Legacy (auto-migrate); recognized set {4, 8, 30, 40, 70}. Bound check: 70 units x 20 members = 1400, under the 1600 EXE population cap but close — if reinforcement stalls with a full map, check this first. Updated M6 tooltips (zh/en), `data/game_schema.json`, TechDoc zh/en, reverse-engineering docs, `ReinforcementGatePatchTests`, and `verify_split_patches` (P8=70, P9 filter=jnz118+92).
- Verification: Core project built clean at the original handoff. A subsequent isolated TEMP-artifacts validation on the same working tree completed successfully: full Release solution build 0 warnings / 0 errors and xUnit 235/235. Re-apply AI Ultimate (migrates Legacy states) and start a new endless game because saves embed the script.

## Reduce Increase Garrison Size from 40 to 30 (2026-07-17)

- Changed Endless AI M6 / P8's active reinforcement unit threshold from `40` to `30`; original restore remains `4`.
- Added explicit compatibility for the former `40` state: detection reports it as `Legacy`, and the next enabled apply migrates it to `30`. Existing `8` and legacy gate migrations remain intact.
- Updated the M6 tooltip, schema, canonical technical/reverse-engineering docs, and P8 verification expectations to describe the new `30` bound (approximately 600 members at 20 per unit).
- Verification complete: focused P8 tests 3/3; full Release solution build 0 warnings/errors; full xUnit 231/231; `data/game_schema.json` parse and `git diff --check` succeeded.
- Independent audit: `EndlessAi.Core` remains the atomic M2+M3+M4 lifecycle and its focused integration/deadline coverage passed as part of a 15/15 Endless AI run; M6/P8 remains independent.
- Safety issue resolved: P8 is now `P8_ReinforcementUnitThresholdPatch`; detection accepts only the exact original gate and three exact documented legacy gates with recognized thresholds `4/8/30/40`. Unknown gates or thresholds return `Unknown`, direct Apply refuses enable and leaves bytes unchanged, and negative regressions cover both cases.
- Terminology/docs resolved: M6/P8 is consistently described as the reinforcement unit-count threshold rather than the separate active-party cap, and the stale `TechDoc.md` summary now uses the current `30000 ms` reinforcement/scheduler timing.
- Reverse-engineering drift resolved: current P8 changes only the `0x195F8` threshold and preserves the original `0x1960C` condition tail. The two historical bounded tails and the old unbounded tail are documented and implemented only as exact migration inputs, not current output.
- Follow-up verification complete: focused Endless AI tests 18/18; full Release solution build 0 warnings/errors; full xUnit 234/234 with no skips; schema JSON parse and `git diff --check` succeeded.

## Optional Features Category & Move Game Speed (2026-07-17)

- Added a new UI category card "Optional Features" (可選功能分類) which holds successfully verified features that are excluded from the "Enable All" action.
- Moved the "Overall Game Speed (10x Acceleration)" (`chkGameSpeed`) option from the "Experimental Features" card to this new "Optional Features" card.
- Adjusted "Experimental Features" card height to 446px and configured the "Optional Features" card at 110px.
- Updated the system console dynamic flow layout grid to automatically accommodate the new card in its waterfall columns.
- Preserved the behavior where "Enable All" leaves "Game Speed" untouched while "Disable All" reverts game speed back to 1x.
- Validated via successful build and passing all 231 unit/integration tests.

## Separate Villager Speed and Add 5x Option (2026-07-17)

- Separated the villager/civilian speed multiplier from the "All units 2x speed" feature (`UnitMovementSpeed2x`).
- Added a dedicated "Villager movement speed 5x" feature (`VillagerMovementSpeed5x`), applying a 5x speed multiplier to civilian units (`FigZivMan00_Zivilist`, `FigZivWei00_Zivilistin`, `FigTiePac00_Packpferd`).
- Added `VillagerMovementSpeed5x` toggle to the switches card UI and dynamic layout.
- Added comprehensive unit and integration tests covering the separation, 5x application, and detector readback, passing all 231 tests in the suite.

## Merge projects into a single executable (2026-07-17)

- Merged Launcher, Modifier, Map Editor, and Save Manager into a single unified `AgainstRomeModifier.exe` application.
- The `src.Modifier` (AgainstRomeModifier.csproj) project acts as the main program entry point and compiles to `AgainstRomeModifier.exe`.
- Relocated `LauncherForm.cs` and `TechDocForm.cs` from the obsolete `src.Launcher` project to `src.Modifier/UI/`.
- Repurposed the Launcher UI's launch actions from spawning processes (`Process.Start`) to showing dialogs inline on the main UI thread (`ShowDialog`).
- Converted `src.MapEditor` and `src.SaveManager` from standalone executables (`WinExe`) to class libraries (`Library`), referencing them directly in `src.Modifier`.
- Updated `ModifierForm` close button behavior to exit the dialog (`this.Close()`) rather than the entire process, returning user flow seamlessly to the Launcher screen.
- Removed the deprecated `src.Launcher` project from the solution file `AgainstRomeModifier.slnx` and from testing dependencies.
- Re-validated the entire suite under xUnit, ensuring all integration and logic tests pass against the new single-process setup.

## Map editor SDL object copy/delete staged editing (2026-07-16)

- Continued the map editor per the spec's remaining Phase 1 SDL scope: the scene inspector now supports staged **object duplication and deletion** on marker-backed custom maps, in addition to the existing team/relative-position edits. Duplication is deliberately template-based (`SdlSceneObjectAddition` copies every field from an existing `[objectNNNN]` block and overrides only `team`/`pos`), because it is the only add path that cannot invent invalid objdef/resource field combinations; free-form object creation from an objdef list remains future work.
- `SdlSceneEditService.SaveChanges` gained optional `removals`/`additions` change-set parameters. Write order per file is: value edits on original indices → additions (`AddObject`, template fields read before any removal, so copy-then-delete-original acts as a move) → removals in descending original index (`RemoveObject` renumbers immediately, so descending order keeps remaining indices valid). Validation rejects removals/additions referencing nonexistent objects, duplicate removals, team outside -1..15, and non-finite coordinates; the existing custom-marker/`ENDL_005-999`/same-directory filename guards are unchanged and still apply to structural changes.
- `MapEditorForm` stages both operations in memory until Save: new `複製物件` and `刪除物件/取消刪除` buttons under the scene editor panel; pending deletions stay listed in red with a `（將刪除）` suffix and can be toggled back; pending copies appear in green with `（待複製）`, are editable (team/pos) and deletable before save, and are drawn on the 2D/3D canvas at a +64 world-unit offset via display-only negative indices. The 2D/3D canvas renders the effective scene (minus pending deletions, plus pending copies); the list keeps showing pending deletions so they can be cancelled.
- After a save containing structural changes, on-disk `[objectNNNN]` indices are renumbered, so the form reloads the SDL catalog from disk and re-baselines; `還原到本次開啟時` therefore restores to the state as of that save, not the original opening, once a structural save has happened (value-only saves keep the old baseline semantics). Restore/dirty logic and the restore confirmation text now cover pending copies/deletions.
- Tests: three new regressions — duplicate+remove with renumbering (template fields preserved, no `[object0002]` residue), copy-then-delete-original acts as move, and invalid structural change rejection leaves the file byte-identical. Focused MapEditorPhase1 20/20; full xUnit 205/205; Release `.slnx` build 0 warnings/errors; `git diff --check` clean.
- Runtime status: a `%TEMP%` fixture (repo read-only `ENDL_000` copy + marker + `floortex.dat`) launch smoke stayed alive; the computer-use visual QA request was denied this session, so the new buttons are **not** visually verified, and in-game loading of a map after SDL copy/delete remains the user's runtime gate (same as the existing SDL team/pos validation gate in Next Steps).

## Native widescreen / expanded viewport feasibility (2026-07-16)

- Second camera runtime result and +1 restoration (2026-07-17): the user reports the revised minimum zoom `0.5` has no visible effect and that the game only genuinely pulls back at `+1`. This runtime evidence rejects the intermediate-float presentation hypothesis despite the raw-float projection references found statically; treat `0.5` as nonfunctional and do not claim it provides a moderate zoom. The current experiment is restored to minimum `1.0`, with the rejected 0.5 cave recognized as `LegacyZoomHalf` and migrated by one cave write. The UI now states the unavoidable observed tradeoff: +1 expands the battlefield but world units, health bars, and information also become smaller. Any later information-overlay correction needs its own runtime-targeted evidence rather than another fractional minimum. Final validation with the concurrent single-executable worktree passed Release solution build with 0 warnings/errors, xUnit 231/231 with 0 skips, JSON parse, and `git diff --check`.
- First camera runtime result and rejected 0.5 hypothesis: the user confirmed the original `CameraZoomOut1` build successfully shows a much larger battlefield in a normal 1280x1024 window (screenshot `codex-clipboard-56b88983-d1be-488e-a779-79fb16b3b755.png`). HUD/top bar alignment remains normal and the world is genuinely zoomed out, but zoom `1.0` made unit/building sprites and information too small/dense, with newly exposed outside-map black geometry at the lower-left boundary. Follow-up Ghidra analysis of all 19 scalar consumers suggested that raw-float projection at 0.5 might coexist with integer zoom-0 information/LOD paths. That implementation passed xUnit 231/231 and build/static validation, but the later 2026-07-17 runtime result above proved it had no visible effect. Preserve this as a failed static hypothesis, not a current implementation claim.
- Camera zoom-out implementation baseline: the Experimental `CameraZoomOut1` profile/registry/UI path uses a reversible four-site EXE patch. It redirects only the persistent startup (`0x81388`), save-load (`0x8D880`), and mission-script (`0x14C1FA`) calls through a 28-byte code cave at `0x1625C0`, now clamping requested native zoom to a minimum of `1.0` before tail-jumping to the stock setter. Internal temporary cache-generation calls remain untouched. Detection accepts Original, `LegacyZoomHalf`, or current Patched; other mixed/unknown signatures refuse enabling. Install order writes the cave before redirects and restore order removes redirects before clearing the cave. Direct Compat/All restore explicitly invokes the camera restore path. A byte-level regression locks both current compare/replacement operands to `1.0f` and the rejected legacy signature to `0.5f`; see the newer runtime bullet above for the current in-game conclusion.
- Camera-height feasibility evidence: the engine has a native zoom scalar `DAT_00771800` controlled by `FUN_00498a30`. The setter clamps negative input to 0 and upper values to 9; projection code uses `1 / (zoom + 1)`, and terrain/object coordinate paths use `1 << zoom`, so increasing the value is a genuine zoom-out with coordinated render/picking math rather than output stretching. It is also serialized/restored through the game-state load path (`FUN_00498a30(local_20)`) and exposed to mission scripts as `lgcSetEngineZoom` / `lgcGetEngineZoom`. A durable experiment cannot patch only startup because saves or mission scripts overwrite it; the bounded minimum-zoom implementation described above now covers all three persistent setters. Runtime checks still required: selection accuracy, edge scrolling, fog/map bounds, sprite degradation, mission-script compatibility, save/load, and performance.
- Final runtime verification: the user confirmed the revised centered fallback now looks normal in both presentation modes. Fullscreen uses the centered 4:3 fake-fullscreen path; windowed mode uses the normal stock-sized window with correct proportions after clearing `WindowedAttributes`. This closes the centered-presentation objective as runtime-verified. Native 16:9 expanded viewport and higher camera view remain rejected/unimplemented; do not conflate them with this verified fallback.
- Eighth runtime result validates the final split by presentation mode: fullscreen is visually correct as a centered 4:3 stock render, but windowed output is horizontally compressed inside symmetric black bars. The window screenshot shows a normal 1280x1024 outer window while `WindowedAttributes=borderless, fullscreensize` attempts desktop-sized image scaling inside it; that forced-window profile is the remaining cause, not the restored EXE. The centered fallback now keeps `FullScreenMode=true`, `ScalingMode=stretched_ar`, `CenterAppWindow=true`, and `FullscreenAttributes=fake`, but clears `WindowedAttributes`. Expected behavior: fullscreen stays centered as observed; windowed returns to the stock-sized decorated window and is only centered on the desktop. Focused dgVoodoo profile tests pass 4/4, standard Release modifier build succeeds with 0 warnings/errors, JSON parses, and `git diff --check` has only line-ending notices. Windowed runtime retest remains the functional gate.
- Final strategy change after the seventh runtime screenshot: native 1920x1080 EXE patching is runtime-rejected. Fullscreen and windowed both retained the same legacy top-left draw surface, so further wrapper tweaks cannot make that internal viewport native widescreen. The existing serialized key/UI control is repurposed as `高解析度置中（4:3）`: `ExePatchFileContributor` always restores all recognized NativeWidescreen EXE states to Original, while dgVoodoo applies `FullScreenMode=true`, `ScalingMode=stretched_ar`, `CenterAppWindow=true`, `WindowedAttributes=borderless, fullscreensize`, and `FullscreenAttributes=fake`. Install detection recognizes that five-key centered profile so the checkbox remains stable after EXE restoration. Customized configs retain unrelated values. The first focused run had 74/75 pass because a rejection test expected the old missing-key name; the program rejected correctly and the assertion was generalized. Final isolated Release solution build succeeds with 0 warnings/errors, full xUnit passes 217/217 with no skips, standard modifier Release rebuild succeeds with 0 warnings/errors, JSON parses, and `git diff --check` has only line-ending notices. Runtime validation of this final centered fallback is pending; do not resume native EXE widescreen patching without materially new engine evidence.
- Sixth runtime result corrects the interpretation of the fifth screenshot: the centered 1920x1440 4:3 output occurred only in dgVoodoo fullscreen presentation. After the game switched back to a normal 1920x1080 window, the latest screenshot again showed a left/top legacy render region (approximately 1024x768) with black right and lower areas. Therefore the two IGM mode sites did not fix the internal viewport; they only made the fullscreen wrapper's 4:3 fallback coherent. The screenshot has a normal title bar, proving `WindowedAttributes=borderless, fullscreensize` was not active. Root cause in modifier management: if `dgVoodoo.conf` differed from the manifest, `DgVoodooFeature` preserved the entire custom file after the embedded profile had already been transformed, silently discarding the required presentation keys. It now preserves unrelated custom values but applies `DgVoodooConfigProfile` to that custom byte stream before writing it. Regression coverage confirms `FPSLimit=60` survives while both required keys change; focused tests pass 4/4, standard Release modifier build succeeds with 0 warnings/errors, and `git diff --check` reports only line-ending notices. Retract the prior claim that windowed alignment is fixed; this new managed-config correction remains runtime-pending.
- Fifth runtime result (user screenshot `codex-clipboard-468050aa-434a-49ae-8412-78afe7fec659.png`): the active-mode/IGM correction removes the prior left-aligned stale region and produces a visually coherent, centered game/HUD. Exact screenshot size is 2560x1440 and the nonblack game presentation spans x=320..2239, i.e. a centered 1920x1440 4:3 area with symmetric 320 px side bars. This satisfies the user's accepted stock-UI fallback, but it also proves the result is a centered 4:3 presentation rather than a native 2560x1440 or 1920x1080 expanded viewport. Do not claim expanded widescreen world view or higher camera height. Keep the feature Experimental; active-mode/IGM alignment is runtime-verified, while edge scrolling/input alignment across the centered bars, dialogs, minimap interaction, and any separate camera-zoom experiment remain unverified.
- New static root cause for the fourth runtime failure: startup mode `0x22` was active in `DAT_006580bc`, but `FUN_00424b80` at file offset `0x24B80` still returned the persisted old mode from `DAT_006580b0`; IGM/resource callers therefore laid out the screen as the old 1280-wide mode even though the framebuffer and input were 1920-wide. The experimental patch now changes that getter from `A1 B0 80 65 00 C3` to `A1 BC 80 65 00 C3`. The IGM dialog-name selector at `0x41E25` also accepted only mode `0x21`; it now accepts forced mode `0x22` and reuses the stock highest-resolution `dlg_igm12_10` layout. This is intentionally an original-size UI fallback, not a claim of a native 1920-wide UI. The prior four-site runtime build is recognized as `LegacyForcedStaleUi` and migrates using only these two new writes; enable/disable and detector regressions cover original, legacy-unforced, forced-stale-UI, patched, and unknown states. Focused Release EXE model/detector tests pass 63/63; final isolated Release solution build succeeds with 0 warnings/errors, full xUnit passes 217/217 with no skips, standard modifier Release output rebuild succeeds with 0 warnings/errors, JSON parsing succeeds, and `git diff --check` reports only existing line-ending notices. Actual centering, expanded world draw extent, UI alignment, and camera height remain runtime-unverified.
- Fourth runtime result: the new dgVoodoo desktop-sized presentation works in the sense that both fullscreen and windowed now occupy the 1920x1080 output, but both modes render the same left-aligned approximately 1280x1024 game/IGM region with a black right band of about 640 px and a small black lower remainder. Crucially, selected units accept movement orders when the user clicks the black right-hand area. This proves the 1920-wide framebuffer/input coordinate space is active and falsifies dgVoodoo/window sizing as the remaining cause; the engine's scene/UI draw viewport or scissor remains capped at the old 1280-wide logical region. Next work must patch the internal viewport/IGM rectangle path (starting with `FUN_00424c50` and its 1280x1024 case), not the wrapper. Do not describe the current build as visually working.
- Third runtime clue and modifier correction: the user confirmed the game's resizable window is capped at 1600x1200 and cannot be dragged larger on a 2560x1440 desktop. The experimental feature now passes a modifier-managed dgVoodoo window profile through `PatchEngine`: `ScalingMode=stretched_ar` plus `WindowedAttributes=borderless, fullscreensize`, so windowed mode can use a desktop-sized borderless 16:9 window while the game renders the patched 1920x1080 framebuffer. User-customized `dgVoodoo.conf` files remain protected and are not rewritten; the removal path also retains its manifest entry so a later reapply can distinguish it from an unmanaged collision. Focused tests pass 4/4, covering CRLF/BOM preservation and idempotence, unknown config rejection, embedded managed install, and customized-config remove/reapply preservation. Earlier focused cycles exposed and corrected an invalid span-copy overload (`CS1501`), CRLF regex handling (3/4 failed), and a faulty BOM test fixture (1/4 failed). Canonical docs/schema were synchronized. Final isolated Release solution build succeeded with 0 warnings/errors, full xUnit passed 215/215 with no skips, JSON parsing and `git diff --check` succeeded, and the standard modifier Release output rebuilt successfully with 0 warnings/errors. Modifier-side work is complete; desktop-sized presentation and all remaining IGM/runtime behavior still require the user's in-game test.
- Runtime result from the user: the first `NativeWidescreen1920x1080` experiment had no visible in-game effect. This falsified the assumption that replacing only mode ID `0x22` create/identify constants was sufficient. Treat that first three-site build as nonfunctional; the revised forced-startup build below remains runtime-unverified and must not be promoted yet.
- Root cause found and revised implementation added: full Ghidra analysis shows the mode switch does pass selected dimensions into renderer/UI refresh, but the stock resolution callback at `LAB_00427d60` sends only IDs `0..3`; the patched `0x22` branch is unreachable through normal UI. The feature now also patches startup `FUN_00424bd0` at file offset `0x24BD0`, replacing `MOV EDX,[006580B0]; PUSH EDX` with `PUSH 0x22` plus five NOPs. The exact old three-site state is recognized as `LegacyUnforced` and migrates by writing only this fourth site; disable restores either legacy or revised states. Localization/docs/schema were updated. Automated validation and second runtime test are pending.
- Revised focused Release validation passed 67/67 with no skips, covering EXE state/apply/restore, one-write `LegacyUnforced` migration, detector/registry behavior, and category restore. A dedicated detector regression for keeping the legacy state checked during migration was added afterward; full validation remains pending.
- Final revised validation succeeded: focused tests passed 68/68, isolated Release solution build completed with 0 warnings/errors, and full xUnit passed 211/211 with no skips. Second in-game verification of the forced-startup build remains the only functional gate.
- Second runtime screenshot proves the forced-startup route now executes: the game renders a widened battlefield and top UI, but the lower roughly 120 px of the 1024x768 client is black with the dgVoodoo watermark. This is a partial success, not a finished feature. The symptom is consistent with reusing the 1600x1200 `igm16001200` viewport/layout or dgVoodoo preserving a mismatched application surface inside the existing 1024x768 window. Next work must distinguish IGM viewport bounds from wrapper presentation by measuring the screenshot, tracing `FUN_00443f60` / `FUN_00443720` and the IGM dialog rectangle data, and checking the modifier-managed dgVoodoo configuration. Do not promote until the black region, UI anchors, mouse mapping, and world-view extent are corrected and re-tested.
- Exact screenshot measurement: outer window is 1026x800 and the client is still 1024x768; non-black rendering spans rows 33..677 and the black/watermark region starts at row 678 (about 122 client pixels). The requested 1920x1080 mode therefore did not resize the actual application window. Public stock screenshots also show a populated bottom HUD/minimap, so the blank band is not a normal finished layout. Before patching IGM coordinates, determine whether the user intentionally launched windowed and whether the desktop is 1920x1080: the next safe experiment is to make dgVoodoo/window presentation match the internal 16:9 surface, then re-evaluate whether any IGM elements still need relocation. `FUN_00443720` does call `FUN_00424c50` after registering the IGM; that adapter has explicit 640/800/1024/1280 rectangle cases but no 1600-to-widescreen case, so IGM adaptation remains a plausible second-stage issue after the window-size mismatch is removed.
- The user clarified that the goal is a genuinely larger game viewport at 1920x1080 or above (more world visible), not higher texture quality or scaling a fixed 4:3 frame. No installed-game file was accessed or modified during this static investigation.
- Existing dgVoodoo2 settings can force output resolution or enumerate modes, but that alone only proves presentation/scaling capability and must not be described as an expanded viewport.
- Static EXE decompilation shows native 16-bit and 32-bit mode paths for 640x480, 800x600, 1024x768, 1280x1024, and 1600x1200 in `FUN_00424760`; selected width/height are stored in `DAT_0061bf48` / `DAT_0061bf4c` and passed to the rendering/UI refresh path. This is positive evidence that the engine is not intrinsically fixed to one framebuffer size.
- The same EXE explicitly loads resolution-specific IGM UI resources through `igm06400480`, `igm08000600`, `igm10240768`, `igm12801024`, and `igm16001200`. A true 1920x1080 feature therefore requires an EXE widescreen patch plus UI/dialog/mouse-coordinate validation; merely replacing the 1600x1200 mode constants may expand rendering but can crop, misplace, or distort 4:3 UI surfaces.
- Most promising next experiment: on a modifier-managed copy/fixture only, replace the 1600x1200 32-bit mode with 1920x1080, retain the 1600x1200 IGM resource as a temporary fallback, and compare screenshots/world bounds, UI anchors, edge scrolling, selection, minimap, dialogs, and mouse hit-testing. Runtime behavior is unverified; do not claim native widescreen support yet.
- Implementation started at the user's request. Added reusable focused script `tools/re/GhidraResolutionAnalysis.java` to print instruction bytes and decompile the display-mode identification/switch, dialog adjustment, and IGM-selection functions. Its first historical headless run against local project `re_workspace/UnitRangeSpeedAnalysis` failed before analysis because that project had no program named `Against_Rome.exe`; no binary was changed. This project-specific failure is now superseded by the clean one-shot import workflow documented in `decompilation-workflow.md`.
- The historical full-analysis fallback also could not open because `AgainstRomeVillageBuildArea.rep` lacked its `.gpr` marker. This was a pre-analysis project-state failure, not a script or patch result. Resolved operationally on 2026-07-17: import the repo-local RE copy into a disposable `%TEMP%` project with the clean Ghidra installation; do not reuse either stale persistent project.
- Importing the repo-local RE EXE into a disposable project with the Temp Ghidra installation also failed before program analysis because that installation is incomplete (`PkzipRecognizer`, `SevenZipRecognizer`, and processor `.pspec` files are missing). Try the separate repo-local `.codex/tools/ghidra` installation once; if its Unicode-path logging problem prevents import, derive patch sites from the PE bytes directly rather than retrying a broken Ghidra installation.
- The separate repo-local Ghidra installation successfully started importing/analyzing the EXE, but the first call hit the 60-second command timeout without a Ghidra error. Reuse the disposable `%TEMP%/AgainstRomeResolutionAnalysisLocal/ResolutionAnalysis` project and run only the focused post-script with a longer, incrementally monitored timeout; do not restart the full import unless the project lacks the program.
- Focused analysis is now reproducible: a no-analysis import plus the updated script wrote `re_workspace/ghidra_resolution_analysis.log` and confirmed the exact instruction bytes. Implemented core `NativeWidescreen1920x1080` wiring and Experimental UI. The patch replaces only native mode ID `0x22` (1600x1200 32-bit): mode identification at file offset `0x246B3`, both display-create/refresh width-height pairs at `0x249AA`, and the same-length mode text at `0x1DCC76`. Width `1600 (0x640)` becomes `1920 (0x780)` and height `1200 (0x4B0)` becomes `1080 (0x438)`; IGM remains `igm16001200`. Detection requires all three sites to be consistently Original or Patched; mixed/unknown state refuses enable writes. Apply/detect/restore, feature registry/profile, bilingual Experimental toggle, tooltip, tests, and canonical docs are wired.
- Added synthetic state/detection/round-trip/Unknown tests. The first focused test command did not compile because it combined a new `--artifacts-path` with `--no-restore`, so the isolated path lacked `project.assets.json` (`NETSDK1004`). Rerun against the existing restored output first, or restore the isolated path before using it; this result says nothing about code correctness.
- Focused Release validation then compiled successfully and passed 65/65 (`ExePatchModelTests`, `ExeFeatureDetectorTests`, and `FeatureRegistryTests`) using the existing restored output. Canonical docs/schema and full solution validation remain pending; in-game viewport/UI behavior remains unverified.
- Restore-category review found `NativeWidescreen1920x1080` was initially restored in the Stats branch even though it is registered as Compat. The call was moved to Compat and characterization assertions now require Stats-only restore to preserve it and Compat-only restore to clear it. Registry/profile round-trip tests also explicitly carry the new toggle; the corrected behavior passed focused and full validation.
- The first post-correction focused test attempt could not rebuild `src.MapEditor/bin/Release` because the user has `AgainstRomeMapEditor` PID 27824 open and it locks `AgainstRome.Core.dll` (`MSB3027`/`MSB3021`). Do not terminate the user's app; rerun build/test with a restored isolated `--artifacts-path` instead. This was an output-lock failure before tests, not a code failure.
- Isolated post-correction validation under `%TEMP%/AgainstRomeModifier_WidescreenArtifacts` succeeded: the focused EXE model/detector/registry suite plus category-restore characterization passed 66/66 with no skips. Full solution validation remains pending; runtime viewport/UI behavior remains unverified.
- Final modifier-side validation succeeded in isolated Release artifacts: `AgainstRomeModifier.slnx` built with 0 warnings/errors and full xUnit passed 209/209 with no skips. `data/game_schema.json` parses, `git diff --check` is clean apart from expected line-ending notices, and UTF-8 BOM remains on `AI_HANDOFF.md`, `TechDoc.md`, and `README.zh-TW.md`. Static patch/apply/detect/restore/UI/docs work is complete. The installed game was not accessed or modified; actual expanded viewport, IGM fallback layout, mouse hit-testing, edge scrolling, dialogs, and minimap still require user in-game verification before the feature can be called runtime-verified.
- A project-only standard Release build of `src.Modifier/AgainstRomeModifier.csproj` also succeeded with 0 warnings/errors, so the normal `src.Modifier/bin/Release/net8.0-windows` modifier output now contains the feature despite the separately running Map Editor locking its own output directory.

## All-units entire-map vision feature (2026-07-16)

- User requested a new all-unit full-map vision option. Modifier-side implementation and automated validation are complete; actual in-game behavior remains unverified.
- Added experimental `AllUnitsEntireMapVision`: it writes `objdef.dau` `Sirad=30000` for the 43 explicitly supported combat/leader/priest/siege rows plus male/female civilians and packhorses; buildings and unknown rows remain unchanged. The shared data affects all factions, not only the player.
- `Sirad` also gates target acquisition and priest casting distance. Detection requires every supported baseline row to be at 30000 and excludes this intended sight change from `Balance`. Per the user's precedence decision, `RangedRange3x` and `SpellEntireMap` compose first and `AllUnitsEntireMapVision` owns `Sirad` last; ranged weapon columns remain 3x. The overlapping priest state still detects `SpellEntireMap=true`, so a combined apply reads all three toggles back as enabled.
- First focused test command stopped at compile error `CS0103` because the new detector referenced `originalRows` outside its former backup-only block. The list was moved to method scope; focused validation must be rerun.
- After the scope fix, focused Release tests passed 27/27 (`PatcherRoundTripTests`, `ObjdefFeatureDetectorTests`, `FeatureRegistryTests`, and `UnitStatsProjectionServiceTests`) using isolated artifacts. Canonical README/TechDoc/known-patches/schema documentation now records the global all-faction scope, supported-row boundary, overlapping priest behavior, and pending runtime verification.
- Final interaction review found that the default-stats projection initially showed the new 30000 Sight but not the same priest field as 30000 casting distance when only the all-unit feature was selected. `UnitStatsProjectionService` now projects both values for priests, with a regression covering combat Sight plus priest Sight/casting distance.
- Final validation after that correction: Release `AgainstRomeModifier.slnx` build succeeded with 0 warnings/errors; full xUnit passed 201/201; `game_schema.json` parses; `git diff --check` is clean; UTF-8 BOM remains on `AI_HANDOFF.md`, `TechDoc.md`, and `README.zh-TW.md`. The modifier-side feature is ready for in-game testing, but actual fog-of-war behavior, AI target selection/performance, and runtime safety are not yet verified.
- Follow-up precedence change: registry order now places `AllUnitsEntireMapVision` after the ranged/spell toggles, the patcher documents and enforces the final `Sirad` override, detector readback keeps all three enabled, and a three-feature regression proves archer range remains 3x while final sight is 30000. Focused Release tests passed 28/28; final Release solution build succeeded with 0 warnings/errors and full xUnit passed 202/202. Runtime in-game verification remains pending.
- UI visibility follow-up: the user's screenshot showed the older standard Release binary (six experimental toggles), while the latest code had only been built to isolated `%TEMP%` artifacts. Rebuilding `AgainstRomeModifier.slnx -c Release` to the standard output succeeded with 0 warnings/errors. A live UI smoke of `src.Modifier/bin/Release/net8.0-windows/AgainstRomeModifier.exe` visually confirmed the seventh experimental toggle, `所有單位全地圖視野`, directly below the custom-balance toggle. The modifier was only opened for visual inspection; no Apply action or installed-game write was performed.

## AI Ultimate M2/M3/M4 integrated core (2026-07-16)

- User requested integration after the dependency review found that reinforcement scheduling (M2), defeat cleanup (M3), and settlement respawn (M4) form one lifecycle. Implementation is in progress and not yet build/test verified.
- Added `EndlessAi.Core` as the only user-toggleable key for this lifecycle. `EndlessAiOrchestrator.RespawnCore` combines every M2/M3/M4 patch under module ID `Core` and replaces those three modules in `UserModules`; M1, M5, and M6 remain independent. The old module properties and keys remain for compatibility.
- `PatchProfile.NormalizeCompositeValues` migrates any enabled legacy M2/M3/M4 selection to the complete core and synchronizes legacy reads. Core detection treats Ultimate and Legacy (including partial/mixed sites) as selected so the next Apply converges a partial installation to the complete safe core; Original/Unknown remain off.
- WinForms now displays one bilingual "AI 無盡重生核心 / Endless Respawn Core" toggle in place of the three old controls. Enable/Disable All, feature-map validation, layout, README, reverse-engineering docs, and schema were updated. Existing-save repair remains a separate opt-in operation.
- Added `EndlessAiCoreIntegrationTests` for profile migration, orchestrator grouping, full M2/M3/M4 application/detection, and partial legacy M3 -> complete Core migration. Focused AI/registry tests passed 29/29 before the final migration case was added; final full xUnit passed 199/199 (therefore includes that case). Release `AgainstRomeModifier.slnx` build succeeded with 0 warnings/errors, `game_schema.json` parses, and `git diff --check` is clean. The manual `verify_split_patches` project compiled and started, but its required golden source directory `C:\遊戲原始檔案` is absent on this machine, so that external-data run stopped before round-trip execution; do not claim it passed. Runtime UI/in-game revalidation remains pending.

## ESAVE_000 paired-save result: outer scheduler clock is rolled back (2026-07-16)

- The user loaded the 2026-07-15 `ESAVE_000`, waited, and overwrote the same save at 2026-07-16 07:21. The type-1 party state did not advance: party types remain `[0,1,1,0,...]`, teams remain 1/2, places remain 2/1, and `v63[1]` remains 2. Object counts changed slightly, proving the simulation and scripts ran; this was not a no-op resave.
- The decisive value is the third scalar in the `CLAK/scr.dat` save callback payload at decompressed offset `0x80`: `3,917,589`. EXE flow `FUN_0052f860 -> FUN_005098d0` snapshots the current script clock into `DAT_02662648`; `FUN_0052fd70` serializes/loads it. The save's outer endless dispatcher deadline is still `ak_level v16 = 9,378,294`. The dispatcher at BCI `0x1BC64..0x1BD38` requires `s_getTime() >= v16` before calling the settlement/reinforcement spawners, so it is currently closed by `5,460,705 ms` of game-clock time.
- Installed `Against_Rome.exe` read-only verification shows both game-speed constants at 10x (`0x204214 = 10,000,000,000`, `0x20424C = 10,000,000`). At a sustained 10x clock, the current save therefore needs about 546 real seconds (9m06s) more before the outer dispatcher can run; at 1x the equivalent is about 91 minutes. The embedded script's outer initial/refresh literals are both `30,000`, so once the stale deadline is crossed normal retries should resume every roughly 3 real seconds at 10x.
- This supersedes the prior weather-task clock inference. `ak_wetter v8 = 9,395,235` is another persisted future timestamp, not proof that current `s_getTime` is near 9.4M. The paired save proves the immediate non-respawn cause is the clock/deadline mismatch, while the previously documented CPU-remnant settle-place veto remains a separate late-game degradation after the dispatcher resumes. The mechanism that rolled the persisted script clock behind its absolute deadlines is not yet proven; do not attribute it to base-game save/load or the 10x patch without a controlled 1x/10x reproduction.
- The user then kept the game running and overwrote `ESAVE_000` again at 07:49. Read-only parsing confirms the predicted recovery occurred: active party slots increased from 2 to 8. Slots 0/3/4/5/6/7 are new; party types are now `[1,1,1,1,3,4,5,3,0,...]`, and `v63` reports four type-1, two type-3, one type-4, and one type-5 party. New type-1 settlements occupy teams 5/6 at places 3/5; the type-4/type-5 pair uses team 4 at place 6. This is concrete saved-state proof that the outer dispatcher resumed after the old deadline was crossed.
- The 07:49 save is now at both intended bounds: `v63[1] = 4 == v70`, and eight total active parties equals the bounded active-party limit. No immediate ninth party is expected until an existing party is released. The serialized callback clock is `4,841,383` while the refreshed `v16` is `14,161,135`, so the save again persists a future deadline (`+9,319,752 ms`). The clock/deadline persistence mismatch therefore remains the likely reason a later load can look stalled again even though runtime spawning itself works; its underlying cause remains unproven.
- Source-level clarification for the user's "is accelerated reinforcement ineffective?" question: the feature is active. Current P3 changes the military reinforcement wait literal from `180000` to `30000`, and P6 changes all six outer scheduler loop ranges to `30000..30000`. These values only control the next deadline written after execution reaches those branches; they do not rebase an absolute `v16` already serialized into a save. On load, `s_getTime() < v16` exits before the accelerated branch can run. The newly saved type-5 party and the increase from two to eight active parties prove the accelerated spawn path works once the stale outer gate opens.
- Implemented a bounded P6 save/load repair without touching the installed game or live save. The fixed-size outer block at code-stream `0x1BC44..0x1BD48` now uses `s_getTime() >= v16 || v16 > s_getTime() + 60000`; only a deadline more than 60 seconds ahead is treated as rollback, then `v16` is refreshed to `now + 30000`. Normal 30-second throttling, the eight-party bound, and the 20 NPC-job limit remain intact. The previous literal-only 30-second P6 state is detected as Legacy and migrated.
- Existing saves can be repaired explicitly in Save Manager via the new bilingual "Repair Endless AI Timer" action. It creates a full ZIP backup first, finds the unique embedded `MAPS/ENDL_###/SCRIPT/ak_level` BCI in `CLAK/scr.dat`, applies the same P6 migration, verifies the PFIL round trip, and writes atomically. Normal Apply never rewrites saves. Focused P6/save tests pass 8/8; characterization/cache/cleanup integration tests pass 15/15; all five local ENDL RE samples pass byte-exact Original -> Ultimate -> Original. Final Release solution build succeeded with 0 warnings/errors and full xUnit passed 194/194; `game_schema.json` parses and `git diff --check` is clean. Runtime in-game revalidation remains pending; the installed game and live `ESAVE_000` were not modified during implementation.

## CI locale-dependent MapSelectionForm title test fix (2026-07-15)

- CI run `29419207320` on `主要開發` (commit `9556b34`) failed with exactly one test: `MapEditor3DTests.MapSelection_form_constructs_with_preview_layout` expected `Against Rome 地圖選單` but got `Against Rome Map Editor - Selection`. CodeQL on the same commit succeeded, so the workflow-path repair itself is confirmed working.
- Root cause: the bilingual feature commit `783d5cf` made `Loc` fall back to `CultureInfo.CurrentUICulture` when no settings file exists; the GitHub Windows runner is English, so `MapSelectionForm.ApplyLanguageToUI` produced English titles/buttons while the test asserts Traditional Chinese. This is an environment dependency in the test, not a product regression.
- Fix: added internal `Loc.OverrideLanguageForTesting(Language)` (sets `_currentLanguage` directly, never writes the user's `settings.json`, unlike the `CurrentLanguage` setter) and pinned `TraditionalChinese` in the two form-title tests in `MapEditor3DTests`. Only these two tests assert `Loc`-driven UI strings; other Chinese assertions in the suite are catalog data, not localization.
- Verification status: this Linux session cannot build the WinForms targets — the Ubuntu apt .NET SDK lacks `Microsoft.NET.Sdk.WindowsDesktop` and the network policy blocks the official SDK download hosts — so verification ran on GitHub. On `claude/github-error-resolution-9ifcdy` commit `d2d2c59`, CI run `29420495112` and CodeQL run `29420495196` both completed with conclusion success. The fix is verified green on the branch; `主要開發` stays red until this commit is merged there.

## GitHub Actions modular-project path repair (2026-07-15)

- Push `783d5cf` fails in both CI run `29417646283` and CodeQL run `29417646317` because the workflows still build the deleted root `AgainstRomeModifier.csproj` after commit `55452b1` split the repository into `AgainstRomeModifier.slnx` and `src.*` projects.
- CI also builds `tests/verify_split_patches`, whose project reference still targets `../../AgainstRomeModifier.csproj`; its current failure is `MSB9008` followed by `CS0246` for `AgainstRomeModifier` and `EndlessAiOrchestrator`. The ordinary test project itself builds successfully before this stale auxiliary reference fails, so this is project-path drift rather than a test regression.
- Repaired CI and CodeQL to restore/build `AgainstRomeModifier.slnx`, retargeted `verify_split_patches` to `src.Core/AgainstRome.Core.csproj`, and retained compile-only CI coverage for that manual golden verifier. Both workflows now install .NET SDK 10 explicitly because `.slnx` CLI support starts at SDK 9.0.200; product target frameworks remain `net8.0`.
- Local verification matching the workflow passed: solution Release x64 build 0 warnings/errors, verifier Release x64 build 0 warnings/errors, and full xUnit 186/186. Remote checks remain unverified until these local changes are committed and pushed; do not claim GitHub is green before the new push runs finish.

## Runtime-verified Bilingual Localization (2026-07-15)

- Added comprehensive Traditional Chinese and English bilingual capabilities to all modules in the repository: Core, Launcher, TechDoc viewer, Save Game Manager, and Map Editor.
- Shared settings persistence was added to `Localization.cs`, storing configuration in `C:\Users\nojac\.gemini\antigravity-ide\settings.json`.
- The user can toggle between English and Traditional Chinese at runtime via UI buttons. All labels, menus, properties, columns, alerts, and diagnostic boxes update dynamically.
- Verified compilation and correctness, successfully passing all 186 unit tests.

## Current ESAVE_000 late-game AI respawn diagnosis (2026-07-15)

- Read-only analysis of the only installed save, `SAVE/ESAVE_000` (`ENDL_002`, game time `16621804`); no installed-game or save file was modified. The save-embedded `ak_level` already contains Ultimate P17/P18/P19 (`100`, unit-scan comparand `1`, radius `800`), so this is not a stale/new-game patch issue.
- The earlier exhaustion/stall theories are contradicted by the save: only party slots 1/2 are active (14/16 free), their CPU teams are 1/2 (five CPU teams unoccupied), `v63[1]=2 < v70=4`, relevant NPC teams have 19/20 job slots free, Siedler probability state is enabled (`v71=1`), and the `ak_level` task is in a normal five-second wait with an advancing clock.
- Resolved all eight `placesSettle` object references through `objects.dat` and `position.dat`, then reproduced the P18/P19 veto inputs. Places 1/2 are occupied by the two live village centers; place 4 is within 800 of the player's live village center; places 3/6/7 are vetoed by 16 team-6, 51 team-3, and 17 team-5 objects respectively. These latter teams have no active party slot, proving late-game CPU remnants still consume settle-place eligibility under P18. Places 0 `(4576,1184)` and 5 `(10464,1312)` have no valid village center or team 0..7 unit within 800 and are not claimed by a live party.
- Therefore the snapshot proves severe late-game settle-place degradation, but not a complete deterministic stop: two valid places and multiple eligible teams remain, so the next Siedler poll should still be able to create a party. Do not claim P18/P19 fully fixed or identify the current symptom as slot exhaustion. If the same save runs for more than one polling cycle without a new party, the next evidence must be runtime tracing/paired saves around the Siedler wrapper (`0x18828` -> type-1 create wrapper `0xE704`), because the captured static gates are open and cannot explain a permanent stop by themselves.

## Decoupling code-review fixes (2026-07-15)

- Reviewed the full `4acd542` decoupling commit line-by-line on `claude/decoupled-code-review-3ly42q` (fast-forwarded from `codex/decouple-core-and-file-workflows`) and applied the surviving findings: `TroopPresetCodec.Write` now uses invariant shortest round-trip formatting instead of `0.##`, so exported presets keep more than two decimals (regression added to `TroopPresetCodecTests`); the `TroopPresetForm` export loop now checks every `TryReadValidatedCell` result like the apply path instead of discarding them; `SaveBackupService.RestoreBackup` no longer embeds a Chinese prefix in the temp-cleanup warning (the UI's `LogRestoreBackupCleanupFailed` prefix was being doubled) and restored the pre-decoupling double-failure semantics — a rollback failure is reported through a new optional `onRollbackFailure` callback (UI logs the legacy `還原存檔失敗後回復原存檔也失敗:` message) while the original restore exception is rethrown instead of an `AggregateException`; `ModifierForm.Data.cs` builds `BuildCurrentPatchProfile` once per default-stats reload rather than once per unit row.
- Intentionally NOT changed from the review: `CleanEparaBaseline` trailing-whitespace trimming (comment-only, epara is never read back for detection, and restoring it would reintroduce `git diff --check` noise) and `TeamDatFeatureDetector` keeping partial evidence plus always running the RomanEndless EXE reconciliation on exceptions (both are improvements over the monolith).
- Verification status: this session has no local .NET SDK; `git diff --check` is clean and the changes are syntax-reviewed only. Build and full xUnit must be confirmed by CI on the pushed branch before claiming green.

## Typed feature catalog decoupling (2026-07-15)

- Completed the recommended UI/file-boundary follow-up. `SaveBackupService`/manifest models now own save scanning, preview reads, ZIP creation, transactional restore, cache invalidation, and deletes; `ModifierForm.SaveManager.cs` dropped from 617 to 247 lines and retains only DataGridView/dialog/log coordination. `TroopPresetCodec` owns 4/6/9-column compatibility parsing and six editable-field serialization, while `UnitStatsEditorService` removes `TroopPresetForm`'s direct dependency on `ModifierForm` and centralizes independent-field normalization. Temporary-directory tests cover create/scan/preview/restore/delete, legacy manifest-less ZIP names, unsafe names, and rejection of a ZIP without `save.ini` while preserving the existing save; codec tests cover all supported row shapes and round trip. A pre-commit focused run exposed a hidden test-order dependency: several core readers called `Encoding.GetEncoding(1251)` before `Program` or another test registered the code-page provider. Core readers now consistently use `PatchText.GameEncoding`, whose static initializer registers the provider; the formerly isolated failure now passes 5/5. Full isolated xUnit passed 186/186; isolated Release build completed with 0 warnings/errors; `git diff --check` is clean; handoff remains UTF-8 BOM. The first normal Release-path command and a `UseAppHost=false` retry were blocked by manifest-elevated smoke PID 35644 locking the EXE/DLL, so build/test validation used SDK `--artifacts-path` under `%TEMP%`; do not mistake this lock for a code regression. No new UI smoke was launched because the preceding elevated smoke remains visible and cannot be terminated from this lower-integrity terminal.
- Completed the final identified decoupling pass. `BackupManager.BackupFiles` is now a `ReadOnlyDictionary` view; mutation remains behind the internal cache-invalidating `SetBackupFile` boundary used by focused tests. Added `UnitStatsProjectionService`, so the default-stats UI no longer duplicates balance/custom/independent modifier composition, and centralized UI-to-`PatchProfile` construction for Apply and preview. Replaced `PatchFileComposer` target knowledge with eight `IPatchFileContributor` implementations (EXE, cl_script, epara, partgeo, scint, ress, objdef, team.dat) plus an injectable contributor regression. The first full test run compiled and passed 178/179; the new projection test used the nonexistent key `FigKelPri00_Druide`, so the expected priest range was 30000 while projection correctly returned unknown-unit baseline 0. Correcting it to `FigKelPri00_Priester` produced full xUnit 179/179. Final serial Release build completed with 0 warnings/errors and `git diff --check` is clean. UI smoke rendered the full main window and remained responsive; cleanup via `Stop-Process`/`taskkill` was denied because the manifest-elevated process outranks the current terminal, and injected Alt+F4/click input could not cross that elevation boundary. PID 35644 remains open and must be closed manually from its visible window. Final core sizes: `FeatureDetector` 44 lines, `PatchEngine` 138, `BackupManager` 184, and `PatchFileComposer` 28; remaining `ModifierForm.Data.cs` code is WinForms grid/file display orchestration rather than duplicated patch-domain policy.
- Began the next behavior-preserving decoupling pass. Added typed `FeatureKey<T>` declarations as the single source of feature IDs/default values; `PatchProfile` named properties and the WinForms toggle map now consume those keys instead of repeating string literals. `FeatureRegistry` distinguishes ordinary toggles from specialized controls and validates the complete UI toggle map at startup, so a newly registered toggle cannot be silently omitted. Registry tests cover declared-key registration, typed defaults/round trips, and missing/unexpected UI mappings. Verification: focused `FeatureRegistryTests` 5/5 passed; Release build 0 warnings/errors; full xUnit 163/163 passed.
- Extracted EXE-backed detection from the monolithic `FeatureDetector` into `Features/Exe/ExeFeatureDetector`. It owns EXE reading and detection for focus-loss, village range, spell altar, game speed, civilian batch production, and unit recruitment, while returning the RomanEndless EXE state to the coordinator for the existing EXE/team.dat mismatch policy. A synthetic-EXE regression covers every extracted field. Focused detector/integration/characterization verification: 10/10 passed. Final verification for the combined decoupling pass: serial Release build 0 warnings/errors, full xUnit 164/164 passed, hidden startup smoke remained alive after 3 seconds, and `git diff --check` clean. A preceding parallel build/test run produced one transient `MSB3026` DLL-lock retry; the serial rerun was clean.
- Extracted all `cl_script.ini` detection (civilian delay, morale, spell damage/healing/resurrection/radius, and general skills) plus its private regex/scaling helpers into `Features/Ini/ClScriptFeatureDetector`; the coordinator now delegates to it, and the focused test adds `SpellRange3x` apply/detect coverage. The new detector uses only `BackupManager.HasFile/GetBackupBytes` for its baseline dependency. The machine SDK briefly disappeared during this work, then was restored as .NET SDK 10.0.302. Final verification after restoration: focused cl-script/integration/characterization tests 10/10, full xUnit 164/164, and serial Release build 0 warnings/errors.
- Extracted `ress.ini` detection into `Features/Ini/RessFeatureDetector`, covering FreeProduction, FreeUpgrade, and NoSpellCost through the same CSV index ranges used previously. Its first focused run failed only in the new synthetic fixture: the `Ger_Kampf` row had 71 columns while `VolkresIndex.UnitUpgradeStart` begins at 264, so no upgrade fields existed to patch/detect. After correcting the fixture to cover the real index range, focused detector/integration/characterization tests passed 10/10. Final verification: full xUnit 165/165 and serial Release build 0 warnings/errors.
- Extracted the complete `objdef.dau` detection body and its dedicated parsing/rule helpers into `Features/Objdef/ObjdefFeatureDetector`. This preserves the existing ordering for capacity/build/HP/projectile checks, Balance false-positive exclusions, LeaderGlory, speed/range, and priest casting distance. The detector now accesses its baseline through `BackupManager.HasFile/GetBackupBytes`. A direct composer-output regression covers every objdef-backed feature and Balance exclusion. Focused detector/integration/characterization verification: 10/10 passed.
- Extracted team.dat detection into `Features/Map/TeamDatFeatureDetector`, which owns per-file enumeration, four-state reconstruction, unknown-state conservative fallback, and aggregate MaxPopulation/RomanPlayer evidence; the coordinator retains the cross-source RomanEndless EXE/team.dat reconciliation policy. The first focused command stopped at compile because removing the old helper also removed the still-needed `AgainstRomeModifier.Core.Patches` using for `ExeRomanEndlessPatchState` (`CS0103`); after restoring the using, focused team/RomanEndless/objdef/integration tests passed 19/19. Final combined verification: `FeatureDetector` is 79 lines, full xUnit 166/166 passed, and serial Release build completed with 0 warnings/errors.
- Completed detector responsibility extraction: `Features/Bci/BciFeatureDetector` now owns Endless AI module and FoodHealing detection, while `Features/Install/InstallFeatureDetector` owns LanguagePack and DgVoodoo state detection. `FeatureDetector` is now a 44-line target-detector orchestrator plus the intentional RomanEndless EXE/team.dat reconciliation policy; formatting was normalized after the extraction. Focused BCI/install/integration/characterization verification: 14/14 passed. Final verification: full xUnit 166/166 and serial Release build 0 warnings/errors.
- Completed the first Apply-side extraction: `Services/PatchFileComposer` now composes the ordinary EXE/INI/DAU/team.dat target bytes as a dry-run `PatchFilePlan`, while `PatchEngine` retains transactional writes, shared BCI flush, language-pack, dgVoodoo, and restore coordination. The first focused run passed 9/10; the new direct composer test alone failed because it enabled `ProjectileArcHeight` against `BackupZipGameFixture`, whose baseline intentionally lacks `partgeo.dau`, correctly triggering the existing safety exception. After restricting the test to files present in that fixture, composer/Apply/characterization tests passed 10/10 and confirmed composition does not mutate the fixture before transaction commit. Final verification: full xUnit 167/167 and serial Release build 0 warnings/errors.
- Extracted category restore rules into `Services/PatchRestoreService`. It now owns Stats/Compat/Language restoration, EXE preservation rules, original-memory file restoration, team.dat population reset, BCI restore/flush coordination, and install-feature removal. `PatchEngine` keeps its public restore facade and Apply ordering and is reduced to 138 lines. Focused composer/Apply/restore/BCI-cache verification passed 11/11; final verification passed full xUnit 167/167 and serial Release build with 0 warnings/errors.
- Completed the first `BackupManager` decomposition slice with `Services/UnitBaselineCatalog` and `UnitStatParser`. The catalog owns the cached `objdef.dau` unit-row projection, original/balanced/custom baseline selection, and cache invalidation; the parser owns pure weapon damage/reload/range parsing and custom-layer merging. `BackupManager` keeps compatibility facade methods and dropped from 864 to 651 lines. A direct regression confirms `Clear()` invalidates previously parsed unit rows/stats; focused unit/objdef/Apply tests passed 12/12. Final verification: full xUnit 168/168 and serial Release build 0 warnings/errors.
- Extracted ZIP source selection/parsing into `Services/BackupSourceLoader` and required-resource rules into `BackupValidator`. Embedded ZIP still takes precedence over local `Backup.zip`, unsafe rooted/parent-segment entries remain rejected, and `partgeo.dau` intentionally remains outside the general required-resource list. `BackupManager` is reduced to 581 lines. Focused ZIP safety/validator/physical-backup/baseline/Apply tests passed 8/8. Final verification: full xUnit 170/170 and serial Release build 0 warnings/errors.
- Extracted physical `.bak` creation/loading into `Services/GameDirectoryBackupLoader` and EXE/objdef/partgeo baseline-signature checks into `OriginalFileValidator`. The loader retains existing-file `.bak` precedence, first-run `.bak` creation, custom-map exclusion, clean `cl_epara.ini` injection, fail-closed dirty-file handling, and `showError` behavior. `BackupManager` is reduced from 581 to 391 lines.
- A new invalid-payload validator regression initially passed 8/9: `IsObjdefOriginal([1,2,3,4])` returned true because the legacy algorithm treated a successfully decompressed payload with no verified leader row as original. This real fail-open gap is fixed: objdef now requires an explicitly parsed `FigRomAnf00_Anfuehrer` HP value of 400. EXE and partgeo already failed closed. Invalid-payload and known-good baseline regressions now cover both directions; focused physical-backup/validator/Apply tests passed 10/10. Final verification: full xUnit 172/172 and serial Release build 0 warnings/errors.
- Extracted missing-entry recovery into `Services/BackupAutoHealer` and the canonical `cl_epara.ini` text/PFIL generation into `CleanEparaBaseline`. The healer preserves `.bak` preference, partgeo validation/backup, custom-map exclusion, and localized logging, and now reports mutations so `BackupManager` can invalidate its unit-row cache after public auto-heal retries. After removing obsolete imports, `BackupManager` is reduced from 391 to 175 lines. Cache-reset, epara PFIL round-trip, physical-backup, Apply, and ClScript focused tests passed 13/13. Final verification: full xUnit 174/174 and serial Release build 0 warnings/errors.
- Added `Services/PatchOperationRunner` as the UI-neutral transaction executor for Apply and all four Restore flows. It creates/disposes `FileRollbackScope`, runs the mutation on a worker thread, commits on success, and restores/rethrows on failure; `ModifierForm` retains validation, button state, dialogs, toggle reset, and post-success reload. Direct commit/rollback regressions plus Apply/Restore characterization tests passed 11/11; `ModifierForm.Patches.cs` dropped from 391 to 328 lines. Full xUnit passed 176/176 and serial Release build completed with 0 warnings/errors. A hidden startup smoke remained alive after 3 seconds. Its first forced-cleanup attempts returned Access denied, but a final process check confirmed PID 45128 had exited and no smoke process remained.
- Removed the duplicate unit-layer, weapon damage/reload, and maximum-range parsers from `ModifierForm.Data.cs`; both default/current UI tables now consume the same `Services/UnitStatParser` used by backup baselines and objdef detection. A direct mixed melee/ranged slot regression covers damage, reload, ordinary range, and priest sight/range. Focused parser/baseline/objdef/characterization tests passed 12/12; `ModifierForm.Data.cs` dropped to 943 lines. Final verification: full xUnit 177/177 and serial Release build 0 warnings/errors.

## Runtime-verified player batch controls (2026-07-15)

- The user confirmed in-game that `CiviProduce20` makes one residential-tent click produce/queue 20 civilians and that `UnitRecruit20` makes one villager-to-unit conversion click select 20. Both are now formal Resource & Combat Upgrades toggles (`pnlSwitchesCard`) and participate in `BtnEnableAll`; they were removed from `pnlExperimentalCard`. Patch, detection, and restore behavior remain unchanged. Canonical runtime evidence is recorded in `docs/reverse-engineering/known-patches.md` and both READMEs. Verification: Release build 0 warnings/errors, full xUnit 161/161 passed, and `git diff --check` clean.

## RangedRange3x projectile reach fix (2026-07-13)

- Runtime follow-up (2026-07-16): the user supplied a screenshot labelled "弓箭手在最大射程的落點". The visible arrow field is consistent with a substantial residual shortfall if the shooter is the off-screen lower-right group firing toward the red-health-bar units at center-left. This would mean the static-derived `ProjectileInitSpeedFactor=1.62` calibration is still insufficient in the actual max-range case; however, the screenshot alone does not prove shooter direction, the exact enabled-toggle combination, or which modifier build/data was applied. Confirm those three points before changing `InitSpeedReachMultiplier`; do not treat a larger `w*_drad` as proof of corrected ballistic reach.
- User report: with 3× range enabled, bows "根本射不到" even stationary enemies that vanilla hits directly. Root cause is ballistic, not the earlier sight/targeting gap (already fixed): horizontal arrival time is the distance-independent constant `4/ISF` (≈2.67 s) while vertical flight-to-ground is `2·emit/ysub` (bows ≈2.47 s), so every arrow lands at ~92.5 % of the target distance — a **fixed ~7.5 %-of-distance short fall**. Vanilla range keeps that within `w_drad` (arrows 45); tripling the range triples the absolute shortfall to far beyond `w_drad`, so arrows land in front of the target. Per user direction the damage radius (`w_drad`) was **not** touched. Fix: `EparaPatcher` now raises `cl_epara.ini [ProjectileInitSpeedFactor]` by `InitSpeedReachMultiplier` = 1.08 (1.5 → 1.62) whenever `RangedRange3x` is enabled, so `4/ISF` drops to ≈ the bow flight time and the land-short fraction → ≈0 at every distance (also reduces mover dodge via shorter flight time). Wired through `PatchEngine` (`EparaPatcher.GetPatchedBytes` now takes `rangedRange3x`); detection is unaffected (RangedRange3x is detected from objdef range columns, epara is not read back, same as the existing variance patch). The 1.08 factor is static-derived from the reverse-engineered estimates (like `ArcEmitMultiplier`) and needs in-game calibration — the 3× hit window is tight (ISF within ~±0.05 of 1.62 for bows). Added `Epara_range3x_raises_init_speed_...` round-trip test and updated the existing epara test's signature. See `docs/reverse-engineering/projectile-ballistics.md`. NOT YET locally built (no .NET SDK in this session) — CI/dev build to confirm.

## Custom Troop Balance Table (2026-07-13)

- Corrected the custom-troop balance-table task after a scope clarification: `TroopConfig.BalancedUnitStats` is the fixed in-modifier source of the selectable `修改器內建平衡` / `Balanced Base Stats` template and the Balance feature. All 42 entries now contain the original-game values extracted once from the repository's read-only original `SYSTEM/DATA_MP/DEFAULTS/objdef.dau` through the production `BackupManager.GetOriginalStats` field mapping. The template remains a static table at runtime; it does not read the selected game directory. The preset editor and localization source paths were restored to `GetDefaultBalancedStats` / their original labels. Because applying the stock table alone no longer changes `objdef.dau`, detection correctly leaves `Balance` false unless custom values produce a difference; affected characterization/integration assertions were updated. Verification: Release build 0 warnings/errors; xUnit 122/122 passed.

## Standalone Release v1.0.0 (2026-07-13)

- `RomanEndless` v1 (team.dat only) failed runtime validation because the endless-only `dlg_volk` faction selector overwrites team 0 after the map default is read. The v2 implementation also patches the selector setter at EXE offset `0x5bd60` so every dlg_volk path supplies faction id 3 (ROM), while retaining the ENDL team.dat change as a consistent default. The user runtime-verified v2 successfully, so it is now a formal Resource & Combat Upgrades toggle (`pnlSwitchesCard`) and participates in Enable All; its labels no longer say Experimental. Apply/Restore/Detect, Stats-vs-Compat category handling, mismatch warnings, tooltip limitations, and synthetic/real integration tests are in place. Final verification: Release build 0 warnings/errors; 121/121 tests passed; `git diff --check` clean.

- Documented the runtime-verified Roman Endless feature across both READMEs, both technical documents, the feature specification, reverse-engineering index/function note, and `data/game_schema.json`. The canonical usage path is: select game root → enable the formal Resource & Combat Upgrades toggle (or Enable All) → apply → start a new endless game; restore by unchecking and applying or using Restore. The docs also record expected no-highlight flags, Romans' original lack of priest/glory tree, unchanged existing saves, and the Options `swi_volk` manual override boundary. Documentation verification: JSON parses; Release build 0 warnings/errors; 121/121 tests passed; `git diff --check` clean; modified Chinese documents use UTF-8 BOM.

- Fixed the user-reported ineffective `RangedRange3x` behavior: previously the modifier tripled `objdef.dau` weapon `w*_rad1/w*_rad2` values but left `Sirad` sight unchanged, so ranged units could not acquire targets across most of their new range. When `RangedRange3x` is enabled, `ObjdefPatcher` now raises each affected ranged infantry/cavalry/siege unit's sight to at least its new maximum weapon range. `FeatureDetector` recognizes this accompanying sight change before balance detection, preventing a false Balance toggle. Added a direct regression test for the weapon-range + targeting-sight contract and updated the existing no-false-Balance characterization test. Verification: Release build 0 warnings/errors; 108/108 tests passed.

- Fixed the `RangedRange3x` Legionary coverage gap (2026-07-14): English `objnames.put` maps `FigRomSch00_Speer_Schild` to `Legionary`, which is a `hybrid_inf`. The shared `TroopConfig.SupportsRangedRange3x` classification now includes hybrid infantry and is used consistently by `ObjdefPatcher`, `FeatureDetector`, `BackupManager`, the current-stats UI, and characterization checks. The patcher's existing per-weapon damage-type filter still limits scaling to ranged weapons, so the Legionary's throwing spear maximum range and matching `Sirad` become 3x while its melee weapon remains unchanged. Added a Legionary-specific regression. Verification: focused range/detection tests 4/4, Release build 0 warnings/errors, full xUnit 153/153 passed, and `git diff --check` clean for the touched files.

- Created automatic release script `tools/publish.ps1`.
- Built and published both `AgainstRomeModifier` and `AgainstRomeMapEditor` as win-x64 Self-Contained and Single-File targets.
- Merged and packaged the executables into `AgainstRomeModifier_v1.0.0_win-x64.zip` (129 MB) after removing debug files.
- Successfully uploaded the package to GitHub Release `v1.0.0` as a Pre-release.

## Bug Fix and Patch Tuning (2026-07-12)

- Fixed the projectile-arc/building-torch interaction (2026-07-14; game runtime revalidation pending): ordinary ranged units' building torch remains excluded from `RangedRange3x` range scaling (`dtyp=5`, original max range 400). `ProjectileArcHeight` already doubled its `w4_emit=9830400`, but previously omitted the matching `Fackel` partgeo row (`ysub=8126464`), causing the user-observed overshoot. `PartgeoPatcher.ProjectileGeoNames` now includes `Fackel`, so launch velocity and gravity both scale ×2 and preserve the original landing point. The objdef regression now covers the real torch slot, and the partgeo regression asserts `Fackel ysub 8126464 -> 16252928`. Verification: focused range/projectile tests 4/4, Release build 0 warnings/errors, full xUnit 160/160 passed. In-game confirmation remains pending.

- `SpellDamage5x` was runtime-verified in-game (user report) and promoted from the Experimental UI group into Resource & Combat Upgrades. It is now included by `BtnEnableAll`; its existing reversible `cl_script.ini` patch and detection remain unchanged. The canonical runtime-status evidence is in `docs/reverse-engineering/priest-spells.md`.

- `BtnEnableAll` now also enables the toggles previously promoted out of the Experimental group: `RangedRange3x`, `UnitMovementSpeed2x`, `SpellEntireMap`, `SpellRange3x`, and `ProjectileArcHeight`. Note the two spell toggles are independent since the 2026-07-12 rework (`SpellEntireMap` = casting distance via objdef `Sirad` 30000; `SpellRange3x` = spell effect radius via cl_script `[Spells] Radius` ×3) — an earlier mutual-exclusion assumption was stale and briefly left `SpellRange3x` out (user-reported). Experimental toggles remain excluded by design. Release build 0 warnings/errors; 107/107 tests pass.

- `ProjectileArcHeight` runtime-verified and finalized at 2x: the mechanism was confirmed in-game at 10x (user reported success — visibly higher arcs, landing points unchanged), then per user request the shipped factor was set to 2x and the feature was promoted out of the Experimental group into the Switches (Resource & Combat Upgrades) card (experimental card 446 / switches card 542, labels dropped "(實驗性)"/"(Experimental)" and now read "拋射彈道增高 2 倍" / "2x Projectile Arc Height"). Factor history: 1.5x visually indistinguishable → 10x verification success → 2x production value. Single source of truth remains `ObjdefPatcher.ArcEmitMultiplier` (shared by `PartgeoPatcher.ArcYsubMultiplier`); emit cell widths hold up to ×10. BtnEnableAll now includes it (see the newer bullet above); BtnDisableAll clears it. The former standalone `RangedAccuracy` toggle has since been confirmed working in-game and folded into `RangedRange3x` (accuracy is just a fix for shots missing at the tripled range) — its objdef `w*_drad ×2` and `cl_epara.ini [ProjectileVarianceOnMove]` → 0.0 edits are now applied and reverted with `RangedRange3x`, and the separate feature ID / UI toggle were removed. Release build 0 warnings/errors; 107/107 tests pass.

- Moved `UnitMovementSpeed2x` out of the Experimental group into the Switches (Resource & Combat Upgrades) group on the UI panel, and removed '(Experimental)' / '(實驗性)' from its localization labels, adjusting card heights to 494 for both cards.
- Completely decoupled unit movement speed from custom troop stats (including balance layer and custom layer) in `ObjdefPatcher.cs`. The unit speed scale `speedScale` is now simplified to only depend on `options.UnitMovementSpeed2x`, ensuring custom stats can never affect moves, movsf, or bmovs fields.
- Fixed incorrect base movement speeds in `TroopConfig.BalancedUnitStats` (reduced 6.4/5.2 to their correct original baseline of 3.2/2.6). This resolves the issue where "Custom Unit Attribute Balance" incorrectly applied a 2x movement speed boost, and when combined with "2x Unit Speed" incorrectly applied a 4x boost.
- Fixed civilian unit movement speed patch logic in `ObjdefPatcher.cs` to prevent the balance modifier from doubling their speeds when the speed patch itself is disabled.
- Added regression test `Test_Unit_And_Civilian_Movement_Speed_Combinations` to `CharacterizationTests.cs`. All 107 tests passed.
- Fixed the bug where the "Custom Unit Attribute Balance" feature would be falsely detected as enabled after applying 2x Unit Speed and 3x Ranged Range patches.
- Optimized `ObjdefPatcher.PatchUnit` to only write fields to `objdef.dau` when their values actually differ from original (>0.01 delta), preserving original number formats and avoiding float-to-int truncation errors during reload.
- Corrected `FeatureDetector.cs` detection logic to dynamically resolve baseline values via `backupManager.GetBaseStatsForUnit` rather than static-coded expectations (e.g. 3.2, 3600.0) which breaks under custom templates.
- Excluded priest's `Sirad` (Sight) attribute check in `FeatureDetector.cs` unit balance detection to prevent false positives when `SpellEntireMap` alters the priest's range.
- Moved `RangedRange3x`, `SpellEntireMap`, and `SpellRange3x` features out of the Experimental group into the Switches (Resource & Combat Upgrades) group on the UI panel, and removed '(Experimental)' / '(實驗性)' from their localization labels.
- Added regression test `Test_RangedRange3x_And_Speed2x_Only_Does_Not_Trigger_Balance` to `CharacterizationTests.cs`. All 107 tests passed.

## History Consolidation (2026-07-11)

- User explicitly approved consolidation of the 80-commit `主要開發` history into 8 linear milestone commits.
- The user requested detailed Traditional Chinese history entries; all eight milestone commit messages were rewritten accordingly without changing source or documentation content.
- Original tip `566ad4a` is retained locally at `codex/history-before-consolidation-20260711`; the rewritten history is the current `主要開發` tip.
- The code and documentation tree were verified equal before recording this handoff update; the handoff update is the only intentional content difference.
- The user approved publishing and `origin/主要開發` was force-updated successfully with `--force-with-lease`. Fresh verification on the rewritten history passed: Release build (0 warnings, 0 errors) and xUnit (98 passed, 0 failed, 0 skipped).

> Last refreshed: 2026-07-11 (UTC+08:00)
> Branch: `主要開發` tracking `origin/主要開發`
> Working tree at refresh: documentation-only changes in progress; preserve them and inspect `git diff` before editing.

## Current Objective

Build a dedicated map editor that can eventually provide an Age-of-Empires-II-like authored-map workflow, while preserving Against Rome's undocumented-format safety constraints. `docs/map-editor-spec.md` remains the staged implementation guide. Changes are intentionally uncommitted pending user direction.

## Modifier Main Console Layout (2026-07-13)

- Reworked the main console into a responsive four-column masonry layout. Each settings card is placed in the currently shortest column, so the five existing groups distribute compactly without fixed column pairings; the settings area scrolls when the window is too short instead of clipping rows. This is layout-only; no patch feature behavior changed. Verification: Release build 0 warnings/errors; 107/107 tests passed; `git diff --check` clean.

## Map Editor Phase 1 Progress (2026-07-11)

- 4U map-space orientation evidence（2026-07-14，進行中）：新增 local-fixture evidence test，五張 ENDL 提供 2,028 個 `4U`、全部 73 張原版地圖提供 34,187 個樣本。以 transition 第一碼作 logical owner 時，八鄰格第一材質證據遠多於第二材質，支持 canonical pair 第一碼為 owner。第一次只掃 ENDL 的 base-ray 趨勢看似符合數字鍵方向；擴至全地圖並加入硬性方向斷言後，test 在 shape 4 失敗（預期 west second=313，實際 east second=579），shape 6 亦呈反向，證明不能把 BMP texture-space 方向直接套到 `boden.txt` map-space。這項失敗已保留為證據並移除過早方向 assertion。高對比 `4U89` montage 已由原版實際 7×7 鄰域成功產生；像素均色投影顯示其 `x0` 約 0.74–0.79 第二材質、`x1` 約 0.23–0.28、`x2` 約 0.45–0.47，但擴至所有可比 pair 後尾碼中位數並不維持相同順序（0=0.506、1=0.723、2=0.509），所以尾碼不是可全域硬編碼的固定覆蓋率。不得把只對 `89` 成立的規則寫入 exporter；下一步改由同 pair/shape/variant 的原版 map-neighborhood 使用情境反推。第一次 Debug evidence build 另因使用者正開著 `AgainstRomeModifier.exe` 出現 `MSB3026` apphost copy retries，但 DLL build 與 test 成功；未停止該程序，後續改用 Release 避免衝突。

- Native blend foundation milestone（2026-07-14）：`FloorMaterialCatalog` 現在分開 catalog 原版兩材質 `4U` 與三材質 `4T` family，只接受已存在的材質順序、shape 與 variant，並能由 texture-space TL/TR/BR/BL 四角配置編譯 base／`4U`／`4T`；無對應原版 tile 時回傳 null，不降級成硬方格。新增 `TerrainBlendAuthoringMap` 作為 tile-corner authoring grid，可用圓形半徑修改 corner material，整圖 bake 會回傳 texture 與精確的 unsupported tile/四角內容。這些是純模型，尚未接到現有 UI／儲存，所以玩家輸出不變。第一輪圓形測試只放單向 synthetic `4U89` 而正確得到 incomplete；補齊 synthetic reverse family 後通過，但只證明 compiler contract，不能推論原版有 reverse family。後續唯讀實庫查證 `4U89` 有 14 張／八方向、`4U98` 為 0，顯示 pair order/layer 語意仍須解析；兩次後續 PowerShell quadrant-average probe 因 PowerShell array division 型別錯誤失敗，改以原圖 montage 確認同 shape 尾碼有多個像素變體，但尚未證實其 layer 強度語意。驗證：focused MapEditor3D 20/20、Release build 0 warnings/errors、full xUnit 152/152、MapEditor Debug build 0 warnings/errors。下一步必須先解出 canonical pair order 與 map-space orientation，再把 corner grid 接上玩家筆刷；不得直接以 synthetic reverse pair 寫入遊戲。

- Native transition pixel-topology milestone（2026-07-14）：因全 pair 統計否定「尾碼固定代表同一覆蓋率」，產品不再用檔名猜 `V`。`FloorMaterialCatalog(FloorTextureLibrary)` 會直接取每張 `4U`／`4T` BMP 的 TL/TR/BR/BL 象限均色，與該 family 的 base representative 做色距分類，建立實圖導出的 corner topology 與 O(1)「四角配置 → 原生 tile variants」索引；`MapEditorForm` 已改用此實圖 catalog。`TerrainBlendAuthoringMap.Import` 可從現有 `boden.txt` tile 投票重建 corner grid，並分開回報未知 tile index 與相鄰 tile 的 corner vote conflicts，未默默覆蓋證據。本機 fixture regression 確認 `4U89__10` 推得的第二材質角數大於 `4U89__11`；synthetic import 可回烘同一 transition 並能偵測相鄰 base tile 衝突。Focused MapEditor3D 22/22 passed；最終 `.slnx` Release build 0 warnings/errors、full xUnit 157/157、`git diff --check` clean，三份繁中協作／規格文件均保留 UTF-8 BOM。此模型尚未接玩家筆刷／儲存；下一步是設計衝突區保留策略及 stroke rollback/undo，之後才可取代現有逐格 `ResolveTexture` 路徑。
- Validation note：第一次完整 build 指令誤用不存在的 `AgainstRomeModifier.sln`，立即以根目錄盤點確認本 repo 使用 `AgainstRomeModifier.slnx`；該失敗未進入編譯、未修改輸出，後續驗證應使用 `.slnx`。

- Native blend transactional brush milestone（2026-07-14，editor runtime verified／game runtime 待驗證）：新增 `TerrainBlendEditSession`，同一滑鼠 stroke 會同步記錄 corner authoring state 與實際 `boden.txt` native texture；只重烘被修改角點相鄰的 tile，因此未碰到的未知 tile／原圖 corner conflict 保持原檔。若任何受影響四角配置找不到原版 `4U`／`4T`，該次事件會先還原自身角點，再取消並回滾整個尚未 commit 的 stroke，UI 顯示拒絕原因，不產生硬方格或部分寫入。Undo、redo、reset、save baseline 現均由同一 session 控制角點與 tile；2D/3D controls 改為只發送 paint intent，不再先寫代表材質造成瞬間錯圖。MapEditorForm 已從原本 center-cell `ResolveTexture` 路徑切換到依 brush size 的 corner-circle native bake。Synthetic regressions 證實成功筆觸可 undo/redo/reset，且第二次加入無 transition 的第三材質會把先前同 stroke 變更完整回滾；真實 B8/B9 fixture 與 3D computer-use smoke 亦通過（詳見後續項）。最終 `.slnx` Release build 0 warnings/errors、full xUnit 160/160、`git diff --check` clean，三份繁中文件保留 UTF-8 BOM。尚未宣稱原遊戲內混合品質已驗證；下一步由使用者在正式自製 map 儲存並進遊戲比較。
- Visual-smoke follow-up：以 `%TEMP%` 的 `ENDL_000` 複本與唯讀 hard-link `floortex.dat` 開啟 3D，不碰安裝／repo 地圖。第一次單格筆觸被 baker 安全拒絕且儲存仍 disabled，因 hover 立即蓋掉拒絕提示而發現 UI 訊息問題；亦確認舊 3×3/5×5 control 逐格發 event 會在完整筆形形成前產生中間 junction。已改為每個游標位置只送一次 intent，由 form 依 brush size 以 `size / 2 + 0.26` 半徑整批圓形 bake，hover 會保留拒絕訊息。隨後 Release rebuild 僅因本次 smoke 的 `AgainstRomeMapEditor (14464)` 鎖住 apphost 而失敗（MSB3027/MSB3021）；該暫存視窗無 dirty state，已透過 UI 正常關閉，未停止使用者的 `Against_Rome.exe`。必須在關閉後重跑 build/test，再用新 binary 重做視覺 smoke。
- Visual-smoke resolution：在全 B8 暫存地形上畫實庫已知 B9 仍安全拒絕，定位到 transition topology 使用整個 quadrant 均色，會把 `4U89 x1` 的窄第二材質角落稀釋成第一材質。改為每張 BMP 四個實際角落各 1/4×1/4 區塊取樣後，real-fixture regression 證實 3×3 B8 中央 B9 可產生 B9 center 與 `4U89`。第二次 3D computer-use smoke 成功：畫面出現柔和的淺色泥地小區、Save/Undo enabled；Undo 後 Save/Undo disabled 且 Redo enabled，Redo 後狀態反轉；儲存到 `%TEMP%` 成功訊息正常。重新以 `BodenTexturesDocument` 讀取暫存輸出：4087×`4B8___50`、1×`4B9___50`，以及 `4U89__71/20/91/60/40/11/80/31` 各 1，證明不是 editor-only shader，而是 8 個方向的遊戲原生 transition tile。此為 editor/runtime UI 與原生檔案輸出 smoke，尚未啟動遊戲載入該暫存圖；原遊戲品質最終 gate 仍需使用者在正式自製 map 驗證。

- Superseded initial native material-blend investigation log（2026-07-14）：資源數量、`4U`／`4T` quadrant 證據、失敗探查與最新 canonical pair 限制已合併到上方的 Native blend foundation milestone；以該項為準。

- Authoring-format direction（2026-07-14，使用者明確要求）：不要把玩家工作流限制為逐格替換 `boden.txt`；目標是類官方編輯器的材質混合筆刷，而且唯一驗收標準是匯出後在原遊戲中的地圖外觀與行為必須達到原版地圖品質。採「自創 editor source format + native exporter」架構：自訂 `.armmap`（名稱暫定）保存高解析度材質權重／筆刷、語意地表、場景與後續高度資料；儲存／遊戲測試時由 baker 將 authoring state 編譯成遊戲原生 `boden.txt` transition tiles、SDL 與必要圖層。自創格式只是內部實作，不得要求遊戲理解，也不得用較平滑的 editor-only shader 掩蓋 native bake 缺陷；原生 bake 預覽與遊戲內實測才是 source of truth。現有 `4U` 證據可支援兩材質 native bake；`4T` 三材質 junction、原版筆刷混合、真正空白圖所需高度／碰撞／DATA cache 都必須繼續逆向或實驗到能產生原版相容結果，不能以降級方格輸出標記完成。只有另做 EXE/DLL loader patch 才能讓遊戲直接讀 `.armmap`，目前不採為主路徑。

- 3D atlas runtime fix（2026-07-14，已實機驗證）：使用者提供 `ENDL_005` 的「3D 診斷」，確認 OpenGL 3.3 context、`floortex.dat` 與 `boden.bmp` 都正常；實際失敗在 `FloorTextureAtlas.Create` 的固定 atlas 排列。舊算法把欄數限制為 2 的次方，超過 256 個 distinct texture 時由 16 欄直接跳到 32 欄，錯誤地認定 `32 × 144 > 4096` 而拋出 `This map uses too many distinct ground textures for one atlas.`。排列已改成依 atlas 實際每軸 28 格緊密配置，保留 128×128 texture 與 8px gutter，4096 atlas 容量由錯誤的 256 提升至正確的 784；超過真正容量時的新錯誤會列出實際數量與上限。新增 300 種 texture 可排列及 785 種正確拒絕的 regression。Focused MapEditor3D 17/17、Release build 0 warnings/errors、full xUnit 149/149、MapEditor Debug build 0 warnings/errors。兩次可選的 PowerShell fixture 計數探查分別因舊版 `Format-Hex` 無 `-Count` 與 PowerShell 無法載入 net8 shared assembly 而未取得數量，未修改任何 fixture；產品修復以純 layout regression 驗證。使用者隨後提供同一 `ENDL_005` 的實機截圖：3D 按鈕保持啟用、完整地勢／水面／地表 atlas 成功渲染且不再出現容量例外，因此此 atlas 問題的 runtime gate 已通過。截圖中的既有方格材質邊界不作為混合素材／`4T` 驗證證據。

- 3D／空白地圖修復（2026-07-14）：3D 初始化與載入失敗不再靜默吞掉原因；程式現在會分別回報缺少 `boden.bmp`、無法讀取 `floortex.dat`、OpenGL 3.3 context／shader 編譯連結例外，停用不可用的 3D 按鈕、保持 2D fallback，並提供含地圖／資源路徑、OpenGL、OS 與 exception 的可複製「3D 診斷」。地圖選單已將誤導的「新建地圖」改名為「從無盡範本建立」，另列出「新建空白地圖」但明確說明真正空白格式尚未完成高度、碰撞與 DATA cache 的安全驗證，不會用殘留範本資料偽裝空白。新增 UI 語意與 3D dependency regressions；第一輪 focused test 僅因缺少 `System.Windows.Forms` using 發生 `CS0246 Control`，補正後 focused 16/16。最終驗證：Release build 0 warnings/errors、full xUnit 148/148、MapEditor Debug build 0 warnings/errors、`git diff --check` clean，繁中規格文件保留 UTF-8 BOM。尚未宣稱使用者電腦的 3D 問題 runtime fixed；下一步應由使用者重開編輯器並複製「3D 診斷」，才能針對實際 driver/resource 原因繼續修復。

- User-reported editor expectation/regression audit (2026-07-14): the user reports that 3D cannot be entered and that `新建地圖` opens an endless-map copy instead of a genuinely blank authored map. Static diagnosis confirms the second behavior is intentional in current code but mismatched with the player-facing label: `MapSelectionForm.CreateMap` calls `SelectNewMapTemplate`, which always selects the first original `ENDL_*`, then `EndlessMapCloner` recursively copies every source file. No blank-map generator exists. A safe authored blank cannot be claimed by merely flattening `boden.txt` or clearing SDL because the cloned map still contains undocumented binary/cache layers; it needs a separately validated minimal template/generation contract. For 3D, `Map3DViewControl.LoadTextures` returns false for missing `boden.bmp` or unavailable `floortex.dat`, `OnLoad` emits `InitializationFailed` for OpenGL/shader/resource failures, and `MapEditorForm` also catches load exceptions; however both UI handlers discard the exception and silently call `Disable3DView`, so the exact runtime cause is currently unknowable from the UI. Do not claim the user's 3D issue diagnosed beyond these branches until the real exception/resource status is surfaced. Next corrective work should first expose actionable 3D diagnostics, then reproduce using the modifier-launched editor; separately rename the current clone-based command or implement a runtime-validated blank template rather than presenting cloning as blank creation.

- Controlled SDL runtime-validation editor (2026-07-14): the scene inspector now exposes `team` and relative `pos` editing for existing SDL objects only on marker-backed custom maps. `SdlSceneCatalog` retains the exact source filename, `[objectNNNN]` index, and local coordinates; UI edits remain staged in memory until the normal Save command. `SdlSceneEditService` independently requires `MAPS/ENDL_005–999`, the custom marker, safe top-level `.sdl` filenames, valid object indices, team -1..15, and finite coordinates, then writes only changed objects through the same `FileRollbackScope` transaction as briefing/environment/terrain. Original and unmarked maps are rejected. The inspector can restore every SDL edit to the values captured when the map was opened, including after an edited state was saved, so the user can validate in-game and then save the restored state. Focused service/catalog/document/form-construction tests pass 7/7, including commit, committed restoration, uncommitted rollback, original/unmarked rejection, path traversal rejection, exact local-coordinate parsing, and local-fixture WinForms construction. Final automated verification: Release build 0 warnings/errors, full xUnit 147/147, and MapEditor Debug build 0 warnings/errors. A first Debug build exposed two nullable warnings in parent-path validation; the path handling was corrected before final verification. Computer-use visual QA was not run because its launcher accepts only an executable identifier and cannot pass the required `--game <repo fixture>` argument; launching without it could inspect the default installed-game path and would violate the repo safety boundary. Runtime behavior is not yet claimed verified.

- SDL document editing foundation (2026-07-14): `SdlDocument` now exposes case-insensitive settlement/object field models and safe value-level mutations for settlement translation plus object `team`, `pos`, `angle`, `def`, and `namedef`. It can append object blocks, remove them, and renumber all remaining `[objectNNNN]` sections continuously while retaining unknown fields, comments, line endings, PFIL headers, and the existing `SafeFileWriter` path. Synthetic regressions cover empty SDL values (the first parser attempt incorrectly allowed `\s` to cross lines and was corrected to horizontal whitespace), unknown-field preservation, coordinate/definition edits, add/remove/renumber, save, and reload. A local-only regression also copied all eight real `ENDL_000/Endlos_*_Siedlung*.sdl` files from the repository fixture into temp storage, parsed their settlement/object data, performed no-op saves, and confirmed the decompressed text remained byte-identical; CI skips this test when the proprietary fixture is absent. Final automated verification: focused synthetic SDL tests 3/3, real-sample round-trip 1/1, Release build 0 warnings/errors, full xUnit 144/144, and MapEditor Debug build 0 warnings/errors. Player-facing SDL writes remain closed until a controlled custom-map runtime check.

- Deferred visual gates (2026-07-14): per explicit user direction, skip mixed-material boundary combinations and three-material `4T` junction validation for now. The runtime-verified same-material painting result remains accepted and this deferred validation must not block continuing to the next editor milestone. Do not mark mixed-material/`4T` behavior verified; resume only when the user returns to it.

- Solid-brush and transition-ownership correction (2026-07-14): the next runtime screenshot showed a single selected `草地` producing a checkerboard of visibly different square variants and a central apparent hole that could not be painted. Two assumptions were wrong. Base-family variants must not be rotated per cell in the player brush, so `FloorMaterial.PickVariant` now always returns the representative texture. More importantly, a `4Uxy` tile belongs on the first/old-material side while its numeric-keypad shape points the second/new material toward the painted cell; assigning it to the second/new side made visible partial tiles logically count as already painted. Transition ownership/resolution is now reversed accordingly, `ApplyPlayerMaterial` always changes the exact clicked logical cell, and both 2D/3D input controls always emit a paint event once per drag cell instead of skipping when the temporary raw texture equals the brush texture. Final automated verification: focused catalog/transition regressions 2/2, Release build 0 warnings/errors, full xUnit 141/141. User runtime screenshot verification then passed: one continuous `草地` region rendered uniformly, the former central missing-cell case could be filled, and no checkerboard/gap remained. The same-material painting gate is complete; mixed-material combinations and `4T` junctions remain next-stage visual gates.

- Superseded transition-cell repaint attempt (2026-07-14): after the first no-response report, an intermediate implementation treated each `4U` tile as the second material and tried to push the boundary outward when it was clicked again. Focused tests and xUnit 141/141 passed, but the next runtime screenshot disproved the ownership assumption: it produced same-material patchwork and unpaintable-looking holes. Do not repeat this second-side ownership approach; the later solid-brush/first-side correction above replaces it.

- Automatic terrain-edge foundation (2026-07-14): the user's first player-facing palette screenshot showed that friendly names alone were insufficient—the painted result still formed hard square blocks. Static read-only comparison of a high-contrast `4U6A` family established that `4Uxy__dV` is a two-material transition: `x/y` are the base-material codes, the suffix's first digit uses numeric-keypad geometry (`2/4/6/8` half edges, `1/3/7/9` corners), and the last character is a variant. `FloorMaterialCatalog` parses available `4U` pairs/shapes and `MapEditorForm` maintains a logical material grid separate from rendered tile names, recomputing the painted cell plus its surrounding ring into one undoable stroke. The later screenshot established the final ownership rule: `4U` belongs to the first/old side and points the second/new material toward the solid painted cell. Three-material `4T` junctions remain separate follow-up work. Earlier automated verification was focused 2/2 and full xUnit 141/141; actual 3D behavior remains a user-visible runtime gate.

- Player-facing terrain workflow correction (2026-07-14): the user clarified that terrain editing must work like a normal game editor—choose `草地`, `沙地`, `泥土`, or `岩地` and paint—without exposing color composition, texture IDs, variants, or two-material splice pieces. `FloorMaterialCatalog` assigns all 28 statically identified base families player-readable Traditional Chinese names/categories and orders common grass first. `MapEditorForm` removes the expert/raw-texture toggle entirely, searches only friendly material names/categories, auto-selects the first ordinary material, and never falls back to a raw tile ID when sampling a transition cell. Internal stable variant selection remains automatic; the subsequent `4U` milestone now also handles supported two-material neighbor transitions. Earlier verification before `4U`: focused catalog regression 1/1, Release build 0 warnings/errors, full xUnit 140/140, `git diff --check` clean, and modified Traditional Chinese documentation retained UTF-8 BOM.

- Terrain-palette abstraction correction (2026-07-14): user feedback correctly identified that exposing thousands of `boden.txt`/`floortex.dat` tile names treats already-composed map fragments as if they were original painter pigments. Read-only inspection of the local fixture confirmed the distinction: `4B?___5?` entries are solid base-material variants (28 visible material families in the current archive), while `4T*`, `4U*`, `L*B*T*`, and similar names are transition/splice tiles; `AA_Brush*` are patterned brush stamps rather than distinct ground colors. Added pure `FloorMaterialCatalog` grouping and deterministic per-cell selection among each base material's variants. Subsequent player-workflow feedback removed the temporary numbered labels and expert raw-tile toggle: transition tiles are now entirely internal and base families use friendly terrain names. Sampling a base variant selects its material family; painting resolves that family back to a stable per-cell variant in both 2D and 3D. The first PowerShell ZIP inspection attempt only missed the `System.IO.Compression.FileSystem` assembly and was immediately rerun read-only with the correct assembly; no game files were changed. Documentation distinguishes base materials, transition tiles, and brush masks without claiming the still-unverified automatic boundary-selection rules. Earlier verification: focused catalog regression 1/1, Release build 0 warnings/errors, full xUnit 140/140, `git diff --check` clean, and Traditional Chinese documentation retains UTF-8 BOM.

- Phase C automated state validation (2026-07-14): extracted the 2D/3D terrain edit state into pure `TerrainEditHistory` logic with no WinForms or GL dependency. `MapEditorForm` now routes both views through this single source for multi-cell stroke grouping, same-cell coalescing, dirty comparison, undo/redo, reset-to-baseline, and save-baseline commits, while continuing to mirror every resulting cell into `BodenTexturesDocument`, `MapCanvasControl`, and `Map3DViewControl`. Added regression coverage for a repeated-cell stroke, undo/redo, save baseline, reset, a stroke that returns to its original texture, and `boden.txt` save/reload persistence. Review found and fixed one edge case before release: paint A→B→A→C within one stroke previously left a duplicate order entry; a per-stroke seen set now guarantees one consolidated undo change per cell. The first persistence-test compile exposed only a missing `AgainstRomeMapEditor` using directive in `MapEditorPhase1Tests`; it was added before rerunning. Final verification: focused history/persistence tests 3/3, Release build 0 warnings/errors, xUnit 139/139, and the temporary-path startup smoke stayed alive after 3 seconds without accessing the installed game directory. Automated Phase C is complete; visual 3D→2D comparison and in-game terrain confirmation remain user-visible gates.

- Map-entry workflow redesign (2026-07-13): the editor now opens a standalone `MapSelectionForm` before the editing workspace. The selector has separate `自製地圖` and `原版地圖（不含劇情）` tabs, excludes all `KAMP_*` campaign/story maps by stable map ID, and owns load, new-map, explicit `複製到自製地圖`, custom-map deletion, game-path browse, and refresh actions. Its right-side preview panel shows the selected map's detached `minimap.bmp` plus name, ID, and type without leaving the source file locked. `新建地圖` uses an original endless map as its stable template instead of silently copying the current selection; the explicit copy command accepts either an original or existing custom map, creates the next free custom `ENDL_*` slot, and opens the resulting copy. After a map is chosen, `MapEditorForm` opens without the former left-side map catalog; its toolbar shows the current map and a `地圖選單` command that returns through the unsaved-change guard. Original maps remain read-only. `--map` now accepts a map ID and is used as the preferred selector item. A user-reported startup failure after adding the preview was reproduced by the new form-construction test: `SplitContainer.Panel2MinSize` was applied while the control still had its tiny default size, throwing `InvalidOperationException` in `BuildInterface`; setting the initial split size first fixed it. A subsequent copy failure was traced to the UI-generated ` - 副本` suffix: `副` (`U+526F`) cannot be encoded in the game's strict Windows-1251 text files. The suggested suffix is now ASCII ` - Copy`, the new-map fallback is `New Custom Map`, and `EndlessMapCloner` rejects unsupported user-entered names with a clear localized message before copying any files. Final encoding-fix verification: focused tests 2/2, Release build 0 warnings/errors, and xUnit 133/133 passed. Keep the constructor, detached-preview, and CP1251-name regression tests.

- 3D game-style view implementation (2026-07-13): `src.MapEditor` uses OpenTK 4.9.3 plus OpenTK.GLControl 4.0.2 for an event-driven offline 3D terrain view built solely from read-only `boden.bmp`, `boden.txt`, `floortex.dat`, and SDL markers. It provides a floor-texture atlas, directional lighting, water, grid/marker overlays, relief control, camera controls, and safe 2D fallback without widening the established write surface. Initial runtime evidence showed only SDL marker clusters because terrain/water winding faced downward under `CullFace`; the terrain indices and water fan now face `+Y`, with a geometric-normal regression assertion. The next screenshot restored the textured surface but showed little relief. A second static defect was found and fixed: `TerrainHeightField` treated one height-map pixel as one tile, so the 64×64 mesh sampled only the first quarter of the 257×257 source instead of the verified four samples per tile. The height field now maps the complete source grid onto the map's tile dimensions, and ray-picking uses the same corrected bounds. Post-fix verification: Release build 0 warnings/errors, rebuilt xUnit 129/129 passed, and `git diff --check` clean. User visual QA on 2026-07-13 then confirmed the complete textured terrain renders with continuous slopes/valleys and visible perspective; the terrain-relief gate is passed. Water alignment still needs a water-bearing map check, and Phase C painting/save validation remains open.

- Phase 1 briefing/environment completion (2026-07-13): the custom-map properties inspector now edits `briefing_titel_2`, the composite/multiline `briefing_text`, and `briefing_text_teamname0..7` in addition to the existing map title. `PutTextDocument` now round-trips concatenated `.put` string literals and the standard `\n`/`\r`/`\t`/quote/backslash escapes while retaining the original PFIL header and transactional save path. The inspector also exposes the statically documented `WaterWarpShift`, `WaterBumpAmplitude`, `WaterBumpFrequency`, and `FlashPropability` values. Original maps remain read-only; CP1251 encoding remains strict. Verification: targeted MapEditorPhase1 tests 8/8, full xUnit 123/123, Release solution build and MapEditor Debug build both 0 warnings/errors. SDL object writes and Phase 3 height/collision writes remain closed behind their existing verification gates.

- Independent scene renderer milestone (2026-07-12): `boden.bmp` + `Heightmapstep` now drive read-only terrain hill-shading and a `Waterlevel / Heightmapstep` threshold drives a `WaterColor` water overlay; visual QA on KAMP_000 matched the river to its minimap. `SdlSceneCatalog` parses top-level PFIL SDL files, maps `refpos + pos` through the verified 64-world-units-per-map-pixel relation, classifies building/unit/other objects, and renders team-colored scene markers. KAMP_000 produced 138 objects and ENDL_000 613 in a read-only probe; the UI adds a player-facing object toggle and friendly scene summary/list without internal names. The default grouped selection bug was fixed so startup reliably selects the first visible campaign map. The game-launch command is explicitly optional and no longer presented as the primary preview. Initial terrain composition now reuses one Graphics context instead of creating 4,096 contexts. Final verification: Release tests 105/105; full Release and MapEditor Debug builds 0 warnings/errors; `git diff --check` clean; temporary probe removed.

- Mode grouping and camera/relief pass (2026-07-12): the 73-map list now uses visible `ListViewGroup` sections with per-mode counts for custom, campaign, historical, tutorial, endless, multiplayer, and other maps. The offline canvas has mouse-wheel zoom (0.75x–6x) and middle-drag pan. It composites each map's read-only `smooth.bmp`/`emboss.bmp` and a `boden.bmp` gradient-derived hill-shade over real floor textures to restore map-specific terrain lighting without claiming or enabling unverified height writes. Computer-use visual QA against the read-only fixture confirmed the campaign grouping, relief display, and live wheel zoom. Verification: Release tests 104/104; full Release and MapEditor Debug builds 0 warnings/errors; `git diff --check` clean.

- Offline renderer scope expansion (2026-07-12): user requires the editor to show the map independently without launching the game. `floortex.dat` was proven to be a ZIP container with 3,005 entries and direct `boden.txt` basename matches; the editor now reads its real 128×128 floor BMPs and renders every terrain cell from game textures. Added `GameMapCatalog`: the read-only fixture contains 73 renderable maps (KAMP 34, MP 20, HIST 10, ENDL 5, TUTOR 4), all now listed and categorized; any renderable original can be cloned into a custom ENDL slot. Visual QA against the repo's read-only game fixture confirmed campaign variants and real floor textures. `alr.dat` (2,075 entries), `apt.dat` (222), and `shad.dat` (2,674) are also ZIP containers and are the next object/model renderer inputs. Do not regress to minimap-as-editor or require launching the game for primary preview.

- Editing-scene separation (2026-07-12): the central canvas no longer draws `minimap.bmp` as its background. It now renders every 64×64 terrain cell as the editable scene, with changed cells outlined and all painting/sample operations occurring on that scene. The original minimap is displayed only in a separate bottom-right `地圖概覽` navigator. Removed the toolbar mode that replaced the editor with the minimap. A computer-use visual QA pass confirmed the scene, grid, palette, and separate overview are visible together without touching game data. Verification: Release tests 103/103; full Release build and MapEditor Debug build both 0 warnings/errors; obsolete layer/view symbols absent; `git diff --check` clean.

- Added player-facing custom-map deletion. The command is enabled only for marker-backed custom maps, shows a default-No confirmation with map name and slot, refuses original maps in the shared deletion service, validates the exact MAPS child path, temporarily moves the directory while updating the manifest, and restores both directory and manifest if deletion fails. Verification: Release tests 103/103, full Release build 0 warnings/errors, and `git diff --check` clean.

- Player-facing editor pass: removed developer-only affordances and wording from the main UI, including internal texture IDs and disabled unverified height/collision/object tools. The terrain palette is now always visible with friendly style names only. Added 1×1/3×3/5×5 terrain brushes, grid toggle, restore-unsaved-terrain action, a real water-color picker, Chinese gameplay labels, and Ctrl+S/Ctrl+Z/Ctrl+Y shortcuts. The editor continues to write only the established custom-map title, environment, and terrain cells; no new game format was made writable. Verification: Release tests 101/101, full Release build and MapEditor Debug build both 0 warnings/errors, `git diff --check` clean, and hidden startup smoke remained alive after 3 seconds.

- Added `src.Shared/AgainstRome.Shared.csproj`; `GameLZSS`, `FileRollbackScope`, and `SafeFileWriter` now have one shared implementation. The original modifier references it without changing their namespaces or call sites.
- Added `src.MapEditor/AgainstRomeMapEditor.csproj`, producing `AgainstRomeMapEditor.exe`. It accepts `--game <path>` and optional `--map ENDL_NNN`, lists maps, clones a selected map into the next available `ENDL_005..999` slot, and safely edits the Phase 1 briefing/boden.ini fields.
- Shared map core implements actual `ENDL_???` discovery, `.arm_custom_map` + `MAPS/arm_custom_maps.json` registration, temporary-directory cloning, unknown-file byte preservation, briefing title changes, and SDL `name` map-path rewrite. All PFIL rewrites retain their original 64-byte headers and write through `SafeFileWriter`; multi-file property saves use `FileRollbackScope`.
- Map text is strict Windows-1251. A title that cannot be represented by the game encoding fails safely rather than being written as `?`.
- `EndlessAiOrchestrator` now enumerates existing `ENDL_???` directories for `ak_level.bci`/SDL targets and calculates expected map-target counts dynamically. `BackupManager` excludes directories marked `.arm_custom_map` from original `team.dat` baseline / `.bak` handling; map population patching remains unchanged and still applies to all maps.
- Added `MapEditorPhase1Tests`; latest verification: `dotnet test tests/AgainstRomeModifier.Tests/AgainstRomeModifier.Tests.csproj -c Release --no-restore` passed **100/100**. A full Release solution build also passed before this handoff update.
- Manual smoke evidence (user, 2026-07-11): a copied new map appears usable and can enter the game. This validates the slot-discovery and basic load path. The user explicitly deferred AI long-run behavior and save/load verification; do not treat either as verified.
- Phase 2 viewer: `MapCanvasControl` now provides read-only minimap, collision bitmap, and deterministic `boden.txt` 64x64 texture-grid views. It deliberately does not overlay SDL objects: the world-coordinate-to-image calibration is still unverified and must not be guessed.
- First authored-map tool: the texture-grid view now has a material palette and a click-to-paint single-tile brush. It writes only the static-verified `boden.txt` 64×64 table, only after the user saves, only to a `.arm_custom_map` custom map, and through the same multi-file `FileRollbackScope` transaction as map properties. Original maps are now read-only in the editor. `BodenTexturesDocument` has exact-cell parsing/writes and a round-trip test; latest xUnit count is **101 passed**.
- Editor UX redesign after user feedback: the previous nested-tab prototype was replaced with a 1440×900 canvas-first workspace. Maps are always visible at left; the center is the active canvas; the right inspector exposes the material palette and map properties; top command/tool bars expose clone, save, undo/redo, material editing, minimap, and collision preview. The material brush supports click-drag painting, tile hover coordinates, palette search/color swatches, undo/redo, dirty-state indication, and save/discard/cancel prompts. Height, collision-write, and object tools are visible but disabled with explicit validation labels. Release build has 0 warnings/errors, xUnit passes 101/101, and a hidden runtime startup smoke test confirmed the form constructs and remains running.
- Stale-launch diagnosis: the user's screenshot showed the old nested-tab UI because `bin/Debug/net8.0-windows/AgainstRomeMapEditor.dll` predated the newly built `src.MapEditor/bin/Debug` DLL. Debug and Release outputs are now rebuilt and hash-matched. `ModifierForm.MapManager` also resolves the newest development DLL/EXE candidate by timestamp while published builds continue to use the sibling executable, preventing an old copied editor from being launched silently.
- Texture-view correction after visual feedback: the first canvas-first build still rendered every texture ID as a hash-derived random color, producing an unusable multicolor grid. The material editor now draws the real `minimap.bmp` as its base, overlays a restrained 64×64 tile grid, and colors only changed cells. Palette swatches are derived from the average minimap color of all cells using that texture ID. Workspace texture folders contain no matching texture images, so IDs such as `L5B09T1A` remain the only available source labels. The computer-use visual QA pass confirmed the new canvas is a recognizable terrain map rather than a random-color wall.
- Material-tool UX correction after a second user screenshot: raw IDs are no longer the primary workflow. The right inspector defaults to a current-brush swatch plus explicit instructions; right-clicking a map tile samples its material and left-drag paints it elsewhere. Status/brush labels use stable per-map names such as `地表樣式 001`. The raw-ID library is hidden behind an explicit `顯示進階材質庫（內部 ID）` checkbox. A second computer-use visual QA pass confirmed the default inspector is clean and no longer presents an unexplained ID wall.
- Real-render preview boundary: the user requires the preview to be the actual game-rendered scene. The editor now labels minimap/collision views as data/editing backgrounds, not game previews, and exposes `在遊戲中預覽`: it transactionally saves dirty custom-map data, launches `Against_Rome.exe` with the selected game root as working directory, and tells the user which `ENDL_NNN` slot to choose. No verified direct-map command-line argument exists. Static EXE evidence for unreachable mode 9 / `TextureEditor V0.1` is documented in `docs/reverse-engineering/map-formats.md`; it appears to be a procedural texture/debug tool, not yet a safe 3D map-editor entry point.
- Phase 3 static baseline: five original ENDL samples were measured read-only and documented in `docs/reverse-engineering/map-formats.md`. BMP dimensions/channel classes and the 64×64 / 256×256 / 257×257 grid relation are static-verified; `vertex.bmp` height semantics, collision black/white semantics, cache-file authority, and SDL-to-minimap calibration are explicitly unverified. Do not expose any corresponding write control until the document's one-variable-at-a-time runtime experiments succeed.

## Verified Current State

- Architecture: `PatchOptions` is removed. `PatchProfile`, `FeatureRegistry`, `IFeatureModule`, `PatchContext`, and `DetectContext` are the source of truth for Apply/Detect/Restore. `PatchEngine` is the orchestrator; feature-specific changes belong under `src/Core/Features/` and pure transforms under `src/Core/Patches/`.
- Baseline and writes: `Backup.zip` is optional and intentionally untracked. The modifier prefers embedded/local `Backup.zip`, otherwise builds an in-memory baseline from the user-selected valid game root. Never edit installed game files directly during development or tests; exercise the modifier's own apply/restore paths with fixtures.
- BCI: FoodHealing and Endless AI use the shared `BciScriptFile` cache and flush once with `SaveAll`. Do not add direct BCI `File.Write` calls from a feature.
- Custom troops: new `.artroop` exports contain exactly `HP,Dmg,VW,AW,Sight,Relt`. Legacy nine-field files are accepted for import, but Speed, Range, SpellRadius, and priest Sight/casting-distance overlaps are normalized away. Independent experimental modifiers own those values.
- Experimental toggles: `RangedRange3x`, `UnitMovementSpeed2x`, `SpellEntireMap`, `SpellRange3x`, and `ProjectileArcHeight` are registered feature IDs. The former `RangedAccuracy` toggle has been folded into `RangedRange3x` (accuracy is just a fix for shots missing at the longer range): enabling `RangedRange3x` also applies `w*_drad ×2` in objdef and `[ProjectileVarianceOnMove]` → 0.0 in `cl_epara.ini`. Priest casting distance uses `Sirad` (objdef column 24) and `SpellEntireMap` writes 30000; spell effect radius is `cl_script.ini [Spells] Radius` and `SpellRange3x` scales it by 3.
- Endless military mode: safe values are party count `20..20`, reinforcement wait and scheduler interval `30000 ms`, bounded active-party limit `8`, and the 60-second save/load deadline-rollback guard. The runtime has only 20 NPC-job slots. Do not restore the rejected unconditional gate bypass. Village/settlement mode remains separate unless explicitly requested.
- CI: `.github/workflows/ci.yml` builds all three projects on Windows and runs xUnit. `EnableWindowsTargeting` is set for cross-platform restore. The optional GitHub automatic dependency-submission setting may still emit an external opaque `HttpError`; this is not a project build/test failure.

## Latest Local Verification (2026-07-15)

```powershell
dotnet build
# Result: 0 warnings, 0 errors

dotnet test
# Result: 186 passed, 0 failed, 0 skipped
```

## Active Constraints

1. Read `AGENTS.md`, this file, Git status/diff, and recent commits before work.
2. Preserve all uncommitted changes; never reset, checkout, rebase, or commit without explicit user approval.
3. Unknown binary/BCI signatures are not writable. Preserve original/patched/legacy/unknown detection and rollback behavior.
4. Keep project documents UTF-8 with BOM when they contain Traditional Chinese. Check readability with `Get-Content -Encoding utf8` after a rewrite.
5. Keep public source free of original game files, `Backup.zip`, local saves, decompiler caches, and restore payloads.

## Useful Entry Points

| Topic | Primary code/doc surface |
|---|---|
| Feature contracts and categories | `src/Core/Features/FeatureRegistry.cs`, `PatchProfile.cs` |
| Apply/restore transaction | `src/Core/Services/PatchEngine.cs` |
| Backup, ZIP safety, baseline stats | `src/Core/Services/BackupManager.cs` |
| Experimental UI and profile map | `src/UI/ModifierForm.cs`, `ModifierForm.Patches.cs`, `ModifierForm.Data.cs` |
| Endless BCI evidence | `docs/reverse-engineering/endless-mode-ai.md`, `known-patches.md` |
| Projectile and experimental evidence | `docs/reverse-engineering/projectile-ballistics.md`, `priest-spells.md`, `known-patches.md` |
| Test fixtures | `tests/AgainstRomeModifier.Tests/` |

## Next Steps

1. Use the new controlled scene inspector on a marker-backed custom map to change one clearly identifiable object's `team` or relative position, save and verify the map loads and the object changes in-game, then use `還原到本次開啟時`, save again, and verify restoration. Record the exact map, SDL file/object index, before/after values, load result, observed result, and restore result before promoting SDL editing beyond validation scope.
2. Complete the remaining user-visible Phase C gates when requested: on a marker-backed custom map, confirm 3D/2D cell identity, Ctrl+Z/Ctrl+Y, saved terrain in-game, and water alignment on a water-bearing map. Mixed-material/`4T` validation is explicitly deferred and must not block other work.
3. Keep optional Phase D (`shad.dat` billboards) closed until the user explicitly approves it; `.alr` model reverse engineering remains out of scope.
4. Keep new proposals in the canonical technical document or a focused reverse-engineering note only when they contain actionable, current evidence; do not recreate completed-plan or historical-review documents.
